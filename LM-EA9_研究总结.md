# TECHART LM-EA9 转接环固件研究总结（交接文档）

> 更新日期：2026-08-31。本文档汇总此前全部分析成果与结论，供后续对话直接续接使用。
> 工作目录：`d:\work\techart`（Windows / PowerShell 5）。
> **代码与文档仓库：`https://github.com/ypat999/TECHART_LM-EA9_modify`**（整个工作目录入库；后续改动请随提交同步更新本文档）。

---

## 0. 一句话目标

让 LM-EA9（**徕卡 M → Sony E** 自动对焦转接环）+ M 口手动镜头在索尼机身上**能够出绿框合焦确认**，可接受日常对焦精度下降。

## 1. 实验环境与症状

- 机身：**Sony A7R3A（ILCE-7RM3A）**。
- 环：TECHART LM-EA9，当前固件 **v1.8.0**，有 F51 / F57 两档对焦速度拨档（环本地档位，不产生机身侧总线流量）。
- 镜头：佳能 EF 卡口手动头（35mm 广角等），目标场景为对焦点位于画面左/右三分位。
- **主症状（重定性后的准确描述）**：AF 能把镜组带到正确焦点位（图像实际已锐），**但不出现绿框合焦确认，环/镜在焦点位短暂停留后大幅移开继续搜索**。35mm 广角几乎 100% 复现，85mm 稳定。
- 已实测 v1.0 / 1.2 / 1.4 / 1.6 / 1.8 五个版本，**主症状无差别**。
- 对比度对焦实验（A7R3A）：在机身切换对比度对焦路径后，**转接环完全无法对焦，大幅前后拉风箱、无在合焦点停留的迹象**（比 PDAF 模式更糟）。
  - 注：A7R III 系静态照相 AF 本身是 PDAF+CDAF 混合、无纯"关 PDAF"开关；用户切换的最可能是视频档"相位检测 AF：手动/自动"之类菜单 ⇒ 该观察反映的是**混合/纯对比度确认回路**在转接环上的表现。
- 光学背景假说（用户提出，仍然有效）：索尼 PDAF 点对竖线敏感，35mm 像场边缘横向色散糊化竖边 ⇒ 相位置信度不足 ⇒ 阈值不达。此假说与"F2 通告光圈"共同构成 F1.2 补丁动机。

## 2. 交付物与文件清单

### 稳定交付物
| 路径 | 内容 |
|---|---|
| `LM-EA9_固件下载地址.txt` | 全部固件/说明下载 URL |
| `LM-EA9_firmware\bin\` | 7 个官方固件（v1.0.0/1.2.0/1.4.0/1.5.0/1.6.0/1.7.0/1.8.0，SHA-256 见 `下载清单与校验值.txt`） |
| `LM-EA9_firmware\releasenotes\` | 官方更新说明（GBK 短文本；`_extract\releasenotes\*_utf8.txt` 为转码版） |
| `LM-EA9_firmware\TECHART_LST_原始清单.txt` | 服务器产品清单原文 |
| `_extract\lex_constants.h` | **native 参照**：LexOptinal E-mount 镜头控制器模板数组（init01/07/08/09/0A/0B/0D/10、norm05/06 完整帧） |
| `patches\*.bin` | 历史共生成 80+ 个 patch 固件（全量清单与判决见 §10/§11.3j~3v+§17.5，均可由 `_extract\mk_patch_*.{ps1,py}` 再生）。**精简后仓内保留 14 支：`EA9-P23-A5UP`（35mm 档）｜40mm 基准 `V3` 与同效簇 `U3/T3/S3`｜已测 `R1/R2/R3`（14.x，全平）｜`W4/W5`（已判 ✖）｜**现役 `C0~C3`（15.x，★首批代码段 patch：位置账本缩放 ×10 等价/×8/×12/×13，见 §17.5）**；另 ORIG=官方 1.8.0 回滚；**日常档：35mm=P23、40mm=V3** |
| `flash_kit\` | **本地刷写套件**：product\ 目录树 + serve.bat + hosts_add/remove/fix.bat + lsts\ 每 patch 一份清单 + HOWTO.txt（清单切换 = 覆盖 product\TECHART_LST.txt；基准档 = P23，VER 3.5.0） |
| `docs_ref\` | **外网检索固化件**（详见 §14）：LexOptical Google Doc 全文、Tapendra 论文文本提取、LexOptical 镜头侧开源源码全套、sigrok-dumps sony_emount 捕获清单 |

### 分析脚本（`_extract\`，均为 PS5 纯 ASCII）
| 脚本 | 用途 | 已知缺陷 |
|---|---|---|
| `probe_updater.ps1` | exe 静态侦查 | - |
| `updater_strings.txt` | exe 字符串（5334 行） | - |
| `dump_latest.ps1` | v1.8.0 帧表解析器（`param([string]$File)`） | **len 过滤 10~900，漏掉 len=0 帧** |
| `hexdump_range.ps1` | 定点 hexdump（发现 len=0 帧用） | - |
| `ver_diff.ps1` | 7 版本逐 key 帧 diff | 同上 len≥10 |
| `presence.ps1` | 全文件 hex 模式存在矩阵 | - |
| `cksum_scan.ps1` | 帧结构扫描 + **cksum 和校验回代**（`-Bin/-From/-To` 可选） | len 过滤 10~300，不捕 len=0 帧 |
| `verify_lex.ps1` | 从 lex_constants.h 提取 native 帧并验算 cksum | - |
| `mk_patch.ps1` | 生成 P 系列 patched.bin（改点前置断言 + cksum 程序化重算 + 自校验） | - |
| `build_kit.ps1` | 构建 flash_kit 目录树/LST/bat | - |
| `verify_kit.ps1` | 起临时 HTTP(8099) 逐条 HEAD 校验所有 LST 引用的 bin/txt 路径 | - |
| 已废弃 | `key_dump.txt`、`latest.txt`（重定向丢数据） | - |

## 3. E-mount 协议要点（已证实部分）

- 帧格式：`0xF0 + len(LE16, =整帧总长) + class + seq + type + body + cksum(LE16) + 0x55`。
- class：`0x02`=init（枚举期）、`0x01`=normal（运行期）。type 高字节=class，如 0x0201、0x0105。
- **固件内消息表存在 len=0 的特殊帧**（body 直接跟在头后、靠尾部 `0x55` 定界），标准长度扫描会整帧漏掉——v1.8.0 有 4 个：init-typ19（0x4CFC，body=`00 00 00`）、init-typ1B（0x4D23）、init-typ28（0x4D37）、class00-typ16（0x4D6A）（0x4CFC 一处系本轮对照 P5 尸检记录时 hexdump 复核补入）。
- **★cksum 算法已破解**：`cksum(LE16 @ frame[len-3..len-2]) = Σ frame[1 .. len-4] & 0xFFFF`——从 len 低字节起累加到 body 末尾（排除 0xF0 头、cksum 自身 2 字节、尾字节 0x55）。证据：native 8/8 命中（verify_lex.ps1 程序化验证，含 init08=0x1A9F）；TechArt 表 11/16 命中。
- **失配的 6 帧 = "改 body 未重算 cksum"的模板残留**（norm06#1、init07、init0B、init3F、t34、init0A#2）。典型法证：init0B body `60 00→60 20`(+32) 但 ck 仍 native 的 0x0078（现 sum=0x0098=78+32）；init0A#2 body 全清零但 ck 仍 0x01E2。⇒ **机身（至少对这些帧）不校验 checksum**——这解释了 len=0 帧 cksum=0000 也能工作。保险起见，自制 patch 一律写回按规则重算的正确 cksum（校验与否两种模型下均安全）。
- init0A 帧与 native（LexOptinal）**逐字节相同，连 checksum（E2 01）都相同** ⇒ 天工照抄标准模板。
- 总线拓扑：E-mount 协议只走卡口 pogo 针（**物理层为 UART**，8N1、750kbps 协商后升 1.5Mbps，机身主机，见 §14.1；旧记录曾误写"SPI"——该误写源自 Neto-Zeme 笔记，已被 §14.4 排除项证伪）；**底座 USB（VID 0483 / PID 575A, HID）是 PC↔环的独立刷写通道**，不承载机身总线流量；F51/F57 档位由环 MCU 本地读取、不在任何总线留痕，差异只体现在环对机身的应答内容/模板选择。
- 光圈编码：`norm05 body[24..27]`，值 = 100×f²。TechArt=`90 01`×2（400=F2.0）；native=`96 00`×2（150≈F1.22）。F1.2 ⇒ 144=`90 00`；更激进 100=F1.0、90=F0.95。
- **协议同一性与 init01 语义的三方独立佐证见 §14（2026-08-31 外网检索，含真镜头样本对照表）**。

## 4. v1.8.0 固件帧表（`bin\v1.8.0_EA9-VER-1-8-0.bin`，len=20172，表区 0x49D4~0x4DDx）

| 偏移 | 帧 (cls/seq/typ) | body 十六进制（非零段） | 备注 |
|---|---|---|---|
| 0x49D4 | 01/08/06 norm06#1 | `000000100030103010 00160000200000...`（len=0x30=48B 整帧、body 39B） | **焦点位置=body[2..3]=`00 10`(0x1000 恒定)**；版本变异字节在 body[10]（旧称 @9=0x16） |
| 0x4A0C | 02/--/01 init01 | `FF9F607D80070850`+24×00 | 能力声明 |
| 0x4A38 | 02/--/07 init07 | `0203080100 001810A0EA 0000000000 6092865E 00600020...`（34B） | @6=0x18 版本 nibble；`60 92 86 5E` 疑似魔数（native 同） |
| 0x4A70 | 02/--/0B init0B | `6020` | native body=`60 00`(96)；**尾 `78 00`=cksum 0x0078，不是第二个字段**（破解前误读，见 §3 法证：TechArt 改 body 为 `60 20` 未重算 ck） |
| 0x4A7C | 02/--/0D init0D | `01`(ck=0x001A) | native body=`00`；**尾 `19 00`=cksum 0x0019**（同类误读订正） |
| 0x4A88 | 02/--/08 init08#1 | `00130019`...@20=`40`? 详下（201B） | 见下 |
| 0x4B5C | 02/--/09 init09 | `0101000000000000000000` | 帧尾 `21 00`=cksum。native body=`0002...`，与 TechArt `0101...` 字节和巧合相等（均+2），故两边 ck=0x0021 都成立 |
| 0x4B7C | 01/64/05 norm05 | 见 §5（96B） | **F 字段 @body[24..27] ⇒ 文件偏移 0x4B7C+6+24=0x4B9A** |
| 0x4BE8 | 02/--/32 | 全 0 | |
| 0x4BF8 | 02/--/4C | 全 0 | |
| 0x4C08 | 02/--/3F | `00`+`TECHART LM-EA9`（65B） | 镜头名（v1.5 前为 `EF40mm f/2.8 STM`） |
| 0x4C54 | 02/--/3D | `01`+0（63B） | |
| 0x4C9C | 02/--/5A | 仅@47=`70`（55B） | |
| 0x4CFC | 02/--/19 (len=0) | `000000` | 本轮复核补录；P5 尸检断言的保护区起点 |
| 0x4CDC | 02/--/34 | `0000007E 52035203 00000000000102100025...`（23B） | `0x0352`=850 |
| 0x4D08 | 02/--/0A init0A#1 | `FF7F0000000000003F00000000000000` | **与 native 逐字节相同（含 cksum E201）** |
| 0x4D23 | 02/--/1B (len=0) | `60116011 0000000A0A01` | 4448,4448——疑似内部马达驱动参数 |
| 0x4D37 | 02/--/28 (len=0) | `00 07 00 FF F401 F401 01 00 00 00 00 09 00 00 00 22 00 00 00 00 00 16 00 0E 00 56 00 00 16 50 00 00 00 00 00` cksum=0000 | 500,500…疑似马达速度/电流表 |
| 0x4D6A | 00/--/16 (len=0) | `0000` | |
| 0x4D74 | 02/--/35 | `4201007F21022602FF0000B0C846432E0D20424626E4F0F02408F4E8EC...`（39B） | 疑似曲线表 |
| 0x4DA4 | 02/--/0A init0A#2 | 全 0（16B） | 第二 profile |
| 0x4DC0 之后 | —— | `42 0F`(3906)、`03 02` 等 | 非帧格式，似配置块 |

init08#1 关键值：`[0..1]=0x1300`(4864)、`[2..3]=0x1900`(6400)、`[7]=0x16`(22)、`[13]=0x20`(32)、`[17]=0x40`(64)、`[20]=0x28`(40，疑 MFD 40mm)、`[29..30]=0x0F94`(3988)。native init08 开头为 `5514 5514`（5205/5205）。

## 5. norm05（0x0105）逐字节对照（核心帧）

```
TechArt: 00 13 00 13 00 00 10 00 07 2A 00 2A 00 54 01 54 01 00 00 00 00 07 80 FF 90 01 90 01 0F 01 00 00 ... 26 00 ...
native : 55 14 55 14 00 00 00 00 07 00 00 00 00 00 00 00 00 00 00 00 00 07 80 FF 96 00 96 00 10 01 00 00 B1 28 47 51 4B 38 27 08 35 30 40 39 00 ... 15 15 00 00 9A 00 0F 37 D4 C6 C9 DE ×2 00
```

| 字段 | TechArt v1.8 | native | 解读（推测） |
|---|---|---|---|
| [0..3] | 4864/4864 | 5205/5205 | 对焦位置量程/刻度 ⇒ **位置一致性假说核心** |
| [6] | 0x10 | 0x00 | 未知（native 恒零） |
| [9],[11] | 0x2A(42) | 0x00 | 未知（native 恒零） |
| [13],[15] | 0x54(84) | 0x00 | 未知（native 恒零） |
| [21..23] | `07 80 FF` | 同 | 相同 |
| [24..27] | **400=F2.0** | 150≈F1.22 | 通告光圈（F1.2 补丁目标） |
| [28..29] | 271 | 272 | 疑焦距/编码 |
| native [32+] | - | `B1 28 47 51 4B 38 27 08 35 30 40 39` | native 独有的距离查表段，TechArt 全零 |

## 6. 跨版本考古（7 个官方 bin 机器 diff + 实测闭环）

### ver_diff / presence 结果
- **const（7 版本逐字节相同）**：init01#1、init0B(6020)、init0D(01)、init08#1、init09、**norm05（含 F400）**、t32/t4C/t3D/t5A、t34、t0A#1/#2、t35、**len0-typ1B/typ28（全版本存在，非某版新增）**。
- **VARIES**：
  - `norm06#1`：v1.0 在 @5@7@9 小改；v1.2~1.7 @9=`60`；v1.8 @9=`16`（native=`3F`）。
  - `init07` @6 = 版本 nibble（1.0→0x11，1.2→0x12，…1.8→0x18）。
  - `init3F`：v1.0~1.4 body=`EF40mm f/2.8 STM`；v1.5+ =`TECHART LM-EA9`。
- **按版本增删的备用 profile**：
  - `init01#2`（`FF9F7F5F80070A00...`）：仅 v1.0、v1.2。
  - `init08#2`（与 #1 差 @13:32↔35、@17:64↔133≈2×）：v1.0~1.5 有，v1.6+ 删除。
  - `norm06#2`（92B，含 `F40F`=4084）：v1.0~1.5 有，v1.6+ 删除。

### 结论
1. 用户实测 5 版本无差别 ⇒ **所有跨版本变过的字段均被判定对主症状（PDAF 绿框）无行为影响**；版本考古路线**关闭**。
2. 主症状的嫌疑字段全部是"7 版本从未变过"的恒量：norm05 的 F400/4864/271/2A/54、init01#1、init07 模板、init08#1、init09/0B/0D、len0 马达帧 ⇒ **版本测试对它们零覆盖**，只能靠自制补丁排雷。
3. 悬案：v1.6 删除 init08#2/norm06#2 双 profile 后，F57 慢速模式改用什么实现？（推测改为代码常量，表内不可见）——不影响主症状分析，留档。

## 7. 症状机理假说（当前最优解释）

绿框 = 机身合焦确认。机身输入只有两类：①自身 PDAF/CDAF 测量；②环上报的数据。环只能操纵②。按机制分层的嫌疑字段族：

- **A. 阈值缩放**：norm05 F 值（400）。机身按通告光圈缩放相位置信阈值、并选 AF 修正表。
- **B. 位置一致性（头号新嫌疑）**：机身校验"环报位置"vs"PDAF 实测像面"。若量程/刻度（4864 vs 5205）、271/272、2A/54 或 init09/0B/0D/init10 编码辅助表任一失配 ⇒ 正好在对焦点时报出距离恒错 ⇒ **不确认、撤回重试**，与症状完全吻合。对比度实验（§8①）加重此嫌疑。
- **C. 判定路径/能力声明**：init01 body 差异字节 @1 9F/97、@2 60/78、@3 7D/15、@5 07/40、@6 08/10、@7 50/00；init07 @0 02/01、@2 08/60、@8..9 A0EA/A030。可能决定机身对第三方环的确认策略。（**勘误**：此前把 init01 帧尾 native `1F 03` vs TechArt `86 03` 当作 body 差异——实为 checksum，两值在各自 body 下均有效，见 §3。）
- **D. AF 驱动应答**：norm06（唯一被官方改过的运行期模板）。
- **E. 运行时计算层**（模板不可见）：环现算的报距与真实像面的偏差——定案需抓包（§11.4 路线 A/B）。
- init0A 距离编码声明与 native 相同 ⇒ 距离编码方案本身是标准的，问题更可能在 B 类量程参数。

## 8. 对比度对焦实验的三条推论

1. 机身肯进对比度路径 ⇒ init01 声明未禁止 CDAF；大幅拉风箱不停留 ⇒ **环跟不上"步进→完成应答→采样"闭环**（norm06 应答、位置量程、typ1B/28 内部马达策略执行链断裂）。
2. 与主症状互洽：若 A7R3A 绿框是混合确认（PDAF 归零 + 对比度分量参与），对比度回路走不通时**即使 PDAF 到位也不出绿框**——且该回路字段（init01/norm05）7 版本恒定，解释了版本间无差异。
3. 判决实验：`norm06@9` 跨版本变过（60↔16，native 3F）但主场景无差异 ⇒ 它不管 PDAF 路；**挂 v1.6（@9=60）再试一次对比度菜单**：行为有变 ⇒ norm06@9 是对比度回路闸门；无变 ⇒ 排除 norm06、火力集中 init01。

## 9. 刷写通道与补丁前提（已查明）

- 升级程序 = .NET 2.0/3.5 WinForms + `hid.dll` P/Invoke + HttpWebRequest 内存流。**不修改 exe**。
- 流程：exe 硬编码拉取 `http://www.techart-logic.com/product/TECHART_LST.txt`，行格式 `TECHART LM-EA9;0483;575A;VER x.y.z;Copyright;<binURL>;<txtURL>`，按 VID/PID 匹配后经底座 USB(HID) 写入。
- **离线刷自制固件方案：hosts 劫持 + 本地 HTTP**——在 `C:\Windows\System32\drivers\etc\hosts` 加 `127.0.0.1 www.techart-logic.com`，用 `python -m http.server 80` 按上述路径结构提供改版 LST（抬高/改 VER 字段以匹配）+ `patched.bin`。exe 无签名校验、无同版本拒刷。
- 安全网：环为 STM32 常驻 bootloader + app 两段结构（bin 无 0x08 前缀，reset 向量 0x6145），刷坏可救。
- **补丁前提已就绪：cksum 算法已破解**（§3），无需再暴力 CRC16。patch 直接改 body + 程序化重算 cksum 写回。两种模型均安全：若机身不校验（有法证支持，§3）——正确/陈旧 cksum 皆可；若校验——只有重算才可行。TechArt 官方自己改 init0B/3F 等帧时未重算 cksum 且产品正常工作，是"不校验"的实证。
- **⚠ 代理会让劫持静默失效**：exe 用 .NET HttpWebRequest，走系统代理 ⇒ 代理侧解析真实域名，拉到的是官方清单（表现为"升级成功"但刷的还是官方固件）。刷前必须关系统代理（Clash/v2rayN/公司代理），并以 serve.bat 窗口出现 GET 日志为唯一判据。
- **⚠ HTTP 根目录必须是 `flash_kit\`**（清单 URL 自带 `/product/` 前缀）。serve.bat 原先 `cd product` ⇒ 全部 404，已修正（`_extract\build_kit.ps1` 同步改）。
- **⚠ hosts_add.bat 首次实跑把条目拼进了上一行**（本机 hosts 末行 `199.232.68.133 raw.githubusercontent.com` 无结尾换行 ⇒ `echo >>` 粘连成 `...com127.0.0.1 www.techart-logic.com`，解析结果是 199.232.68.133 而非 127.0.0.1，本地服务一条请求都收不到）。已修：追加前先 `echo.>>hosts`；并新增 `flash_kit\hosts_fix.bat`（管理员跑，正则剥离任意位置的旧条目+保证换行后重写，自带 hosts.bak_techart 备份）。
- **⚠ 浏览器会缓存该 txt**：实测 hosts 修好前用户仍"看到官方清单"＝启发式缓存（http 纯文本无 Cache-Control）。**判据一律用 `curl.exe http://www.techart-logic.com/product/TECHART_LST.txt` + serve 窗口 GET 日志**，别信浏览器。
- **系统代理状态（本机实测）**：WinINET `ProxyEnable=0`（`ProxyServer=127.0.0.1:10809` 处于关闭态）、winhttp Direct ⇒ 当前不是代理劫持失效那条坑，但仍需在刷写前确认。
- **⚠ "显示 1.8.1"≠刷入证据**：任何 P 系列 patch 都没动 init07 的版本 BCD 字节（frame@0x4A38 body[6]=0x18），设备自读版本恒为 1.8.0；升级程序里的 1.8.1 只是 LST 行回显。为此新增 **P6（VER 1.9.0）=`EA9-P6-P1PROBE.bin`**：P1 光圈 144 + init07→0x19 + init3F 镜头名→"TECHART LM-EA9-P6"，三处落地探针一次刷写即可判定通道通不通（名称进机身镜头信息/EXIF，版本可被升级程序读回）。
- **跨版本取证（`versions_table.ps1`，v1.0~v1.8 共 7 版）**：norm05/init08 的光圈字节 `90 01 90 01` 全家族从未改过；**光圈/身份类字段**官方跨版本只改 init07 版本 BCD（11→12→14→15→16→17→18）与 init3F 名称（v1.4 及以前 "EF40mm f/2.8 STM"，v1.5 起 "TECHART LM-EA9"）——其余跨版本变化（norm06@9、备用 profile 增删）不含任何光圈字节，详见 §6。若 P6 证实已落地而机身仍显 F2.0，则机身"最大光圈"显示源**不在 norm05 body[24]**（嫌疑转向 class02 枚举帧，如 init08 body[24..30] `19 00 00 1C 00 94 0F` 区域；注意机身显示只在**挂载枚举时**读取一次，刷后须关机重装镜头再开机）。
- **已生成（`patches\` + `flash_kit\`，用法见 `flash_kit\HOWTO.txt`）**：每个 patch 一个独立 LST（lsts\ 目录，单行以防点错版本），刷哪个就把对应 LST 覆盖到 product\TECHART_LST.txt；固件在升级程序版本列表中以 VER 1.8.1~4.0.0 呈现（P1~P28，探针版本读回见 §10），ORIG 行（1.8.0）保留官方原固件作回滚；另已本地镜像官方 **v1.6.0**（`lsts\TECHART_LST_V16.txt`，VER 1.6.0）供 §8.3 判决实验离线做。全部 LST 引用的 URL 已用 `verify_kit.ps1` 逐条 HEAD 验证 200（bin 均 20172B）。

## 10. 实验矩阵（单变量；成功判据=目标场景出绿框）

| # | 补丁 | 机制 | 状态（固件已生成，cksum 已重算） |
|---|---|---|---|
| P1 | norm05 F 400→**144**（F1.2） | 阈值缩放 | `EA9-P1-F12.bin`（VER 1.8.1，diff 3B）— ❌ 实测无效（经 P6 探针证实已落地：显示与 AF 均无变化 ⇒ norm05 光圈字段排除） |
| P2a | norm05 F 400→100（F1.0） | 极端放宽 | `EA9-P2a-F10.bin`（VER 1.8.2） |
| P2b | norm05 F 400→90（F0.95） | 极端放宽 | `EA9-P2b-F095.bin`（VER 1.8.3） |
| P3 | init01 body 换 native（@1..@7 六字节） | 换确认路径 | `EA9-P3-INIT01N.bin`（VER 1.8.4，diff 7B，新 ck=0x031F 与 native 一致）— ★**已实测阳性：全实验唯一改变行为者——稍近处合焦成功率略升，极限远无改善 ⇒ 火力转向 init01** |
| P4 | norm05 量程 4864→5205、271→272 | 位置刻度对齐 | `EA9-P4-RANGE.bin`（VER 1.8.5，diff 6B）— 未单测，并入 P9 |
| P5* | init08 整帧换 native（含双 profile 字段） | 位置/马达标定对齐 | `EA9-P5-INIT08N.bin`（VER 1.8.6，diff 86B，高危档）— ✖**已实测炸：认镜头但首按 AF 即通信丢失（读不到版本、不动作）⇒ init08 整帧替换路线永久关闭**。尸检修正：native init08 同为 len210 原地替换、init34@0x4CDC/init19@0x4CFC/init0A@0x4D08 布局完好，**无溢出**；挂因=native 电机 profile 内容（`55 14 55 14`/`79 AB 04`）与 TechArt 标定（`00 13 00 19`）不符，机身首按 AF 下发越界行程。恢复：bootloader 常驻，直接重刷任意其它固件即可 |
| P6 | P1 + 落地探针（读回 1.9 + 镜头名 -P6） | 通道/落地判决 | `EA9-P6-P1PROBE.bin`（VER 1.9.0，diff 9B）— ✅ 已实测：落地成立，光圈仍 F2.0、AF 不变 |
| P7 | init08 光圈候选 3988→1440 + P1 + 探针 | 机身显示源定位 | `EA9-P7-INIT08A.bin`（VER 2.0.0，diff 12B）— ✖**已实测（读回 2.0 落地成立）光圈仍 F2.0 ⇒ 表内光圈候选（norm05=400、init08=3988）全部排除，机身 F 值显示源在静态表之外 ⇒ 光圈显示判据永久废弃，只看绿框** |
| P8* | norm06 @9→`3F`（native）——原表 P5 | AF 应答档位 | **未生成**：先做 §8.3 v1.6 免刷判决实验再定（原表"P5=norm06"已顺延为 P8，避免与落地探针 P6 重名） |
| P9 | **P3+P4 组合** + 探针 | 能力声明+量程对齐能否吃下极限远 | `EA9-P9-P34.bin`（VER 2.1.0，diff 19B）— ✖**已实测（1~1.5m）：差于 P3 ≈ 接近原版 ⇒ P4 量程对齐是负项，组合路线关闭** |
| P10 | init01 @1 单字节 9F→97（P3 二分①，仅 bit3） | 定位 active byte | `EA9-P10-A1.bin`（VER 2.2.0，diff 10B）— ✖**已实测：≈ 原版 ⇒ @1 单独无效（中性）** |
| P11 | init01 @2+@3 `60 7D`→`78 15`（P3 二分②） | 定位 active byte | `EA9-P11-A23.bin`（VER 2.3.0，diff 11B）— ✖**已实测：全场最差 ⇒ @2@3 是 P3 内的有害字节（"毒"），永久排除** |
| P12 | init01 @5+@6+@7 `07 08 50`→`40 10 00`（P3 二分③） | 定位 active byte | `EA9-P12-A567.bin`（VER 2.4.0，diff 12B）— ★**已实测：≈ P3 ⇒ P3 有效组锁定 = {@5,@6,@7}；与 P11 对照证明 P3 = 有效组 + 有害组** |
| P13 | init01 @5 单字节 07→40（P12 二次二分①）+ 探针 | 定位关键单字节 | `EA9-P13-A5.bin`（VER 2.5.0，diff 10B）— 已实测：**略差于 P16 但最边缘处更稳 ⇒ @5 = 边缘相关字节** |
| P14 | init01 @6 单字节 08→10（P12 二次二分②）+ 探针 | 定位关键单字节 | `EA9-P14-A6.bin`（VER 2.6.0，diff 10B）— 已实测：**明显差于 P16 但优于原装 ⇒ @6 弱正、@7 在场时冗余** |
| P15 | init01 @7 单字节 50→00（P12 二次二分③）+ 探针 | 定位关键单字节 | `EA9-P15-A7.bin`（VER 2.7.0，diff 10B）— **★★已实测：优于 P16 ⇒ @7 就是关键字节，P15 = 新冠军/新基准** |
| P16 | init01 @1+@5+@6+@7（**P3 去毒版**：剔除有害 @2@3）+ 探针 | 新最优基准候选 | `EA9-P16-NOP23.bin`（VER 2.8.0，diff 13B）— ★已实测：**优于 P3 ⇒ 去毒正确；但劣于 P15 ⇒ 多字节叠加有稀释** |
| P17 | init01 @5+@7（新冠军+边缘字节）+ 探针 | 最优新组合候选 | `EA9-P17-A57.bin`（VER 2.9.0，diff 11B）— ★**已实测：总体好于 P15，极限边缘也有概率对上** |
| P18 | init01 @6+@7 + 探针 | @6 叠加增益判定 | `EA9-P18-A67.bin`（VER 3.0.0，diff 11B）— 已实测：好于 P20 但不如 P17 ⇒ @6 无增益，出局 |
| P19 | init01 @1+@7 + 探针 | @1 叠加增益判定 | `EA9-P19-A17.bin`（VER 3.1.0，diff 11B）— **★★已实测：成功率≈P17 但合焦更快、总体最好 ⇒ @1 与 @7 协同（单用中性）** |
| P20 | init01 @7 中间值 50→28 + 探针 | @7 值语义探针（连续标定 vs 阈值二值） | `EA9-P20-A7HALF.bin`（VER 3.2.0，diff 10B）— 已实测：略差于 P15 且不稳 ⇒ **@7 是连续标定值、越低越好，00 为表内端点** |
| P21 | init01 @1+@5+@7（合并双冠军 = P16 去稀释者 @6）+ 探针 | 收官组合候选 | `EA9-P21-A157.bin`（VER 3.3.0，diff 12B）— 已实测：**不如 P19 且偶发走太远脱框 ⇒ @1 与 @5(40档) 互相有害** |
| P22 | init01 @7=FF（越过 native 端点）+ 探针 | 有符号语义探针 | `EA9-P22-A7FF.bin`（VER 3.4.0，diff 11B）— 已实测：**与 P19 体感类似 ⇒ FF 与 00 等效（回绕相邻低端皆可）** |
| P23 | init01 @5=80+@7=00（@5 推过 native 40）+ 探针 | 边缘字节上限探针 | `EA9-P23-A5UP.bin`（VER 3.5.0，diff 11B）— **★★★已实测：又准又快 ⇒ 新冠军（不用 @1 也快）** |
| P24 | init01 @1=17+@5+@7（@1 再清一位）+ 探针 | 提速位甜点位探针 | `EA9-P24-A1DN.bin`（VER 3.6.0，diff 12B）— 已实测：快但合焦不稳 ⇒ **@1=97 甜点位定案** |
| P25 | init01 @5=A0+@7=00（@5 爬档第1步）+ 探针 | @5 上限夹逼 | `EA9-P25-A5A0.bin`（VER 3.7.0，diff 11B）— ★待测 |
| P26 | init01 @5=C0+@7=00（@5 爬档第2步）+ 探针 | @5 上限夹逼（与 P25 一轮夹出最优档） | `EA9-P26-A5C0.bin`（VER 3.8.0，diff 11B）— ★待测 |
| P27 | init01 @1+@5=80+@7=00（@1 叠加新冠军）+ 探针 | 高 @5 下 @1 是否重新有用 | `EA9-P27-A157UP.bin`（VER 3.9.0，diff 12B）— ★待测 |
| P28 | init01 @5=80 单独（@7 保持 50）+ 探针 | 对照：高 @5 下 @7=00 是否仍必要 | `EA9-P28-A5ONLY.bin`（VER 4.0.0，diff 10B）— ★待测 |
| X1 | **init01 整行照抄 Sigma 15/1.4 真样本** FF 9F 78 5D 82 60 18 7E + 探针 | 真实镜头"机身必收"行 | `EA9-X1.bin`（VER 5.1.0，base=P23 diff 13B）— ✖**M40 实测：无法对焦** |
| X2 | P23 + @2@3=78 5D（Sigma 值）+ 探针 | "@2@3 有毒"是否 35mm 专属 | `EA9-X2.bin`（VER 5.2.0，diff 7B）— ✖**M40：表现不如 P23 ⇒ @2@3 有害跨镜头成立** |
| X3 | P23 + @4=82（Sigma 值）+ 探针 | 三样本中唯一未探差异位 | `EA9-X3.bin`（VER 5.3.0，diff 7B）— ✖**M40：无法对焦 ⇒ @4=80 是 TechArt 正确值，不可动** |
| X4 | P23 + init08[13] 20→E1 + 探针 | §14.4 队列头名（两真样本皆 E1） | `EA9-X4.bin`（VER 5.4.0，diff 8B）— ✖**M40：无法对焦 ⇒ init08[13] 整条路线关闭** |
| X5 | P23 + init08[13]=E1 且 [17]=15 + 探针 | 同上，双字节 | `EA9-X5.bin`（VER 5.5.0，diff 9B）— ⛔**停用**（含已被 X4 证伪的 [13]=E1；[17] 单字节改由 X11 承担） |
| X6 | P23 + norm05 [9][11]/[13][15]（2A/54）清零 + 探针 | native 恒零的 TechArt 独异字段 | `EA9-X6.bin`（VER 5.6.0，diff 13B）— ★待测 |
| X7 | P15 字节组 @5=40+@7=00 + 探针 | 换镜头后重找基准（低 @5 锚点） | `EA9-X7.bin`（VER 5.7.0，diff 7B）— ★待测（M40 优先） |
| X8 | P23 + norm05 F 400→144 + 探针 | F 字段排除结论在 M40 上未验证 | `EA9-X8.bin`（VER 5.8.0，diff 8B）— 待测（低优先） |
| X9 | P23 + @7=7E（真镜头值）+ 探针 | @7 轴在 M40 上的另一端点 | `EA9-X9.bin`（VER 6.1.0，diff 8B）— ≈**M40 实测：约等于 P23 ⇒ @7 轴在 M40 上平坦**（35mm 上"越低越好"不迁移） |
| X10 | P23 + @6=10（native 值）+ 探针 | @6 在 35mm 弱正，M40 未验 | `EA9-X10.bin`（VER 6.2.0，diff 8B）— ★**M40 实测：略好于 P23 ⇒ @6=10 升为 M40 新基准（至今唯一正信号）** |
| X11 | P23 + init08[17]=15 单字节 + 探针 | 隔离 [17]（[13] 已由 X4 判死） | `EA9-X11.bin`（VER 6.3.0，diff 8B）— ≈**M40 实测：约等于 P23 ⇒ init08 通道零增益，整条路线关闭** |
| X12 | @6=10 + @5=A0（@7=00 保持）+ 探针 | M40 新基准上重开 @5 爬档 | `EA9-X12.bin`（VER 6.4.0，diff 9B）— ✖**M40：不如 P23** |
| X13 | @6=10 + @5=C0 + 探针 | @5 上端夹逼 | `EA9-X13.bin`（VER 6.5.0，diff 8B）— ✖**M40：不如 X12（@5 越高越差）** |
| X14 | @6=10 + @5=40 + 探针 | @5 下端锚点 | `EA9-X14.bin`（VER 6.6.0，diff 9B）— ✖**M40：不如 P23、略好于 X12** |
| X15 | @6=18（Sigma 值）单独 + 探针 | @6 轴上端 | `EA9-X15.bin`（VER 6.7.0，diff 8B）— ≈**M40：不错、≈P23** |
| Y1 | P23 + norm05 body[32..37]+[39..43] 照抄 native 距离表段（保留活字节 body[38]=0x26）+ 探针 | **新帧**：距离↔位置编码表 | `EA9-Y1.bin`（VER 7.1.0，diff 18B）— ★**M40 实测：有效、对焦变快（差别不大）⇒ norm05 距离表段是活字段，M40 首个正向增益** |
| Y2 | P23 + init07 body→native（@0/1/2/5/7/8/9/10，@6 留作版本探针）+ 探针 | **新帧**：驱动类型/行程声明 | `EA9-Y2.bin`（VER 7.2.0，diff 11B）— ✖**M40：相位区框大幅缩小、对焦点可移出框外、全画面易绿框但实际虚焦（假合焦）⇒ init07 实时控制机身 AF 区域/合焦逻辑，但 native 值方向错误** |
| Y3 | Y1 + Y2 合并 + 探针 | 两新帧叠加 | `EA9-Y3.bin`（VER 7.3.0，diff 25B）— 待测（预期被 Y2 的假合焦污染，价值低于单测） |
| Z0 | Y1 + @6=10（两大正信号叠加）+ 探针 | 增益堆叠冠军候选 | `EA9-Z0.bin`（VER 8.1.0）— ✖**M40：≈Z2、边缘难合焦**（叠加 @6=10 反而变差，不成立） |
| Z1 | P23 + init07 **@2 单字节 08→60** + 探针 | 拆 Y2：@2 数值跳变最大（8→96），疑为行程/驱动类型 | `EA9-Z1.bin`（VER 8.2.0）— ★**M40：框同官方、较好梯队、边缘略好、速度快 ⇒ @2 是安全正信号、与框无关** |
| Z2 | P23 + init07 **@9+@10 EA 00→30 C7** + 探针 | 拆 Y2：@8..A 像 LE16 量程（0x00EA=234→0xC730=51000），疑为 AF 声明范围 | `EA9-Z2.bin`（VER 8.3.0）— ✖**M40：框同、边缘极难合焦 ⇒ @9A 量程对方向错误（负效果）** |
| Z3 | P23 + init07 **@0/@1/@5/@7 小标志位组**（02→01,03→01,00→01,10→00）+ 探针 | 拆 Y2 剩余：标志位集合 | `EA9-Z3.bin`（VER 8.4.0）— ⚠️**M40：框很小、比 Y2 更接近合焦但仍虚；关键异常"镜头还在移动就绿框、先绿框后镜头才到位、似无反差对焦验证" ⇒ 此四字节组内含"合焦验证流程/判定阈值"控制位，是绿框判定的直接开关** |
| Z4 | P23 + init07 **@0 单字节 02→01** + 探针 | 拆 Z3：@0 是否控框/控验证 | `EA9-Z4.bin`（VER 8.5.0）— ⚠️**M40：框小、提前绿框、虚焦 ⇒ @0=合焦判定/反差确认位（01=跳过确认直接绿→虚；02=严格确认→M40永不绿=原始症状）** |
| Z5 | P23 + init07 **@1 单字节 03→01** + 探针 | 拆 Z3：@1 是否控框/控验证 | `EA9-Z5.bin`（VER 8.6.0）— ★★**M40：框正常、反差拉风箱收敛、会真合焦、慢、边缘难、不提前绿 ⇒ @1=反差收敛模式位，01 档首个"能真合焦"的发现（方向=官方 03 在 M40 上永不绿框）** |
| Z6 | P23 + init07 **@5 单字节 00→01** + 探针 | 拆 Z3：@5 | `EA9-Z6.bin`（VER 8.7.0）— ★**M40：框正常、≈Z1 挺不错 ⇒ 非关键位（安全）** |
| Z7 | P23 + init07 **@7 单字节 10→00** + 探针 | 拆 Z3：@7 | `EA9-Z7.bin`（VER 8.8.0）— ★**M40：框正常、≈Z1 挺不错 ⇒ 非关键位（安全）** |
| V1 | **Y1（norm05 距离表段）单独重验边缘** + 探针 | 唯一"全像面"杠杆的边缘单测 | `EA9-V1.bin`（VER 10.1.0）— ✖**M40 边缘实测：不如原版 ⇒ 距离表单独使用为负项（早前"变快"伴随不稳，边缘判据下出局）** |
| V2 | Y1 + norm05 量程 4864→**5205**（P4 档）+ 探针 | 距离表 + 量程对齐（位置一致性） | `EA9-V2.bin`（VER 10.2.0）— ✖**实测：不如原版 ⇒ 量程 5205 为毒（与 Z2 的 init07 量程改大失败互证："量程改大=毒"两代成立）** |
| V3 | Y1 + norm05 [28..29] 271→**272** + 探针 | 距离表 + 焦距编码对齐 | `EA9-V3.bin`（VER 10.3.0）— ★**实测：比原版边缘合焦更快 ⇒ 正信号；但活性成分待隔离（272 单独？还是必须表+272 联动？）→ U1 专测** |
| V4 | Y1 + @6=10（X10 正信号）+ 探针 | 距离表 + init01@6 | `EA9-V4.bin`（VER 10.4.0）— 待测（低优先，被 U3 取代） |
| V5 | Y1 + @2=60（Z1 提速）+ 探针 | 距离表 + 提速 | `EA9-V5.bin`（VER 10.5.0）— 待测（低优先，@2=60 本轮无法感知） |
| V6 | Y1 + 量程 5205 + 271→272（native 位置对齐全档）+ 探针 | 距离表 + 全位置刻度对齐 | `EA9-V6.bin`（VER 10.6.0）— ✖**实测：不如原版（被毒量程 5205 拖累，对照 V3 可知唯一差异即 rng）** |
| U1 | P23 + norm05 [28..29]=272 **单独**（无距离表）+ 探针 | 隔离 V3 活性成分 | `EA9-U1.bin`（VER 11.1.0，diff 8B）— ≈**M40 实测：与原版无明显变化 ⇒ 272 单独惰性，V3 正信号=距离表本体（但需 272 纠偏）** |
| U2 | P23 + @6=10 + 272（双正信号叠加，无距离表）+ 探针 | 冠军候选 | `EA9-U2.bin`（VER 11.2.0，diff 9B）— ✖**M40 实测：差于原版、边缘更难 ⇒ @6+272 无表为反向联动毒（@6 单独正、272 单独平、合体毒）** |
| U3 | P23 + 距离表 + 272 + @6=10（= V3+@6，全正号顶配）+ 探针 | 上限试探 | `EA9-U3.bin`（VER 11.3.0）— ≈**M40 实测：与 V3 基本一样 ⇒ @6 在表+272 地基上不加分；V3=静态表冠军** |
| T1 | V3 + norm06 焦点位置 body[2..3]=**0x0162**（Techart 初代被机身接受的实证值）+ 探针 | 最后未动帧、唯一全像面位置上报 | `EA9-T1.bin`（VER 12.1.0，base=V3）— ✖**M40 边缘实测：显著弱于 V3 ⇒ 位置字段是活杠杆（机身真消费），但 354 是谷底** |
| T2 | V3 + norm06 位置=**0x0000**（近端零点）+ 探针 | 位置基准另一端 | `EA9-T2.bin`（VER 12.2.0）— ✖**实测：弱于 V3 但好于 T1 ⇒ 曲线非单调（0>354，4096 最优）** |
| T3 | V3 + norm06 位置=**0x1300**（4864=量程声明值，内部一致）+ 探针 | 位置↔量程对齐 | `EA9-T3.bin`（VER 12.3.0）— ≈**实测：与 V3 差不多、略慢 ⇒ 高位平台 [4096,4864]，V3 原生 4096 已在平台顶附近** |
| S1 | V3 + norm06 位置=**0x1455**（5205=native 表的原生刻度端）+ 探针 | 平台右沿外推（表按 5205 刻度设计假说） | `EA9-S1.bin`（VER 13.1.0，base=V3）— ✖**实测：不如 V3 ⇒ "表按 5205 设计"假说证伪** |
| S2 | V3 + norm06 位置=**0x1800**（6144=Sigma 量程顶，远超声明量程）+ 探针 | 超量程是否崩（机身 clamp 行为） | `EA9-S2.bin`（VER 13.2.0）— ✖**实测：低于 V3 但明显好于 S1 ⇒ 不 clamp、但曲线非单调（5205 反常低谷）** |
| S3 | V3 + norm06 位置=**0x0800**（2048，354~4096 中点补测）+ 探针 | 低位谷形态 | `EA9-S3.bin`（VER 13.3.0）— ≈**实测：与 V3 差不多 ⇒ 平台左延至 2048**（后经 §16.2 勘误：当时只改 body[2..3]，[20..21] 仍 4096，属"双拷贝分歧 2048"档） |
| R1 | S3 + norm06 **body[20..21]→`00 08`**（位置双拷贝对齐 2048）+ 探针 | §16.2 对齐假说主测 | `EA9-R1.bin`（VER 14.1.0，base=S3）— ≈**实测：与 S3 无明显区别 ⇒ 对齐假说不兑现正收益（只确认大分歧有害）** |
| R2 | S3 + norm06 **body[0]→02**（真 Sigma limit flags 形状）+ 探针 | flags 单变量 | `EA9-R2.bin`（VER 14.2.0）— ≈**实测：无明显区别** |
| R3 | R1+R2（Sigma 完整形状：flags+对齐双拷贝）+ 探针 | 打包候选 | `EA9-R3.bin`（VER 14.3.0）— ≈**实测："隐隐约约更稳"属噪声级 ⇒ norm06 双拷贝真收官** |
| W4 | P23 + init07 **@1=02（中间值）** + 探针 | **@1 值域缺口：03 严格永不绿 ~ 01 真收敛之间是否有档** | `EA9-W4.bin`（VER 9.4.0，i07@6 nibble 94）— ✖**M40 实测：无显著提升 ⇒ @1 值域扫尽（01 中心收敛慢/02 平/03 永不绿），此位无边缘解** |
| W5 | P23 + init07 **@0=00（低于官方 02）** + 探针 | **@0 确认位值域下沿** | `EA9-W5.bin`（VER 9.5.0，nibble 95）— ✖**M40 实测：机身无法识别接环（链路握手失败）⇒ @0 是能力位非确认旋钮，02 为有效下限，W5 路线（比官方更松）彻底关闭** |
| C0 | **代码段 patch**：fn@0x6D2C 缩放 ×10 改写为 `movs+muls ×10`（等价对照组）+ 探针 | 证明代码段 patch 方法本身无害 | `EA9-C0.bin`（VER 15.1.0，diff 10B，base=V3）— ✔**已实测：与 V3 一致 ⇒ 代码段 patch 通道清白，后续差异皆为真信号（方法学里程碑）** |
| C1 | 同上，缩放 **×10→×8**（`lsls #3`）+ 探针 | **上报位置标定**首档（-20%） | `EA9-C1.bin`（VER 15.2.0，diff 9B）— ✖**已实测 M40：弱一点（×8 方向为负）** |
| C2 | 同上，缩放 **×10→×12**（三段 adds/lsl）+ 探针 | +20% 档 | `EA9-C2.bin`（VER 15.3.0，diff 8B）— ★**已实测 M40：边缘似乎强一点 ⇒ 全程唯一正信号档，待 D 世代确认真实性** |
| C3 | 同上，缩放 **×10→×13**（movs+muls）+ 探针 | +30% 档（@5 有效暗示方向：上报量偏小则上调） | `EA9-C3.bin`（VER 15.4.0，diff 9B）— ○**已实测 M40：无明显差异（×13 已回落 ⇒ 若 C2 是真峰则峰形极窄，噪声假设并存）** |
| D1 | 代码段 patch，缩放 **×10→×11**（movs+muls）+ 探针 | 斜率加密：真伪主探针 | `EA9-D1.bin`（VER 16.1.0，diff 11B，nibble F1）— ✖**实测 M40：不如 C2（真焦）；另报焦距档显示 44mm=40×1.1 ⇒ 焦距共享 ×N 链** |
| D2 | 同上，缩放 **×10→×14** + 探针 | 上探 | `EA9-D2.bin`（VER 16.2.0，diff 11B，nibble F2）— ○**实测 M40：≈V3（显示 56mm）** |
| D3 | 同上，缩放 **×10→×16**（`lsls #4`）+ 探针 | 上探·2 的幂 | `EA9-D3.bin`（VER 16.3.0，diff 10B，nibble F3）— ○**实测 M40：≈V3（显示 64mm）** |
| D4 | 同上，缩放 **×10→×20** + 探针 | 激进边界 | `EA9-D4.bin`（VER 16.4.0，diff 11B，nibble F4）— ○**实测 M40：≈V3（显示 80mm）⇒ 缩放轴正式关闭（§11.3x）** |
| E1 | **代码段 patch·轨迹门开放**：0x6D32 `beq→nop`（fn@0x6D2C 入口门控失效 ⇒ 每次被调都刷新账本×10 到 05 位置槽）+ 探针 | sig_rounds 实证机身 **60Hz 每轮重读位置**、真镜头 05.cur 逐帧收敛 ⇒ 若调用周期性=释放真实行程轨迹 | `EA9-E1.bin`（VER 17.1.0，diff 7B，nibble 61，base=V3）— ✖**实测：无显著差异（§11.3ae；焦距显示金丝雀亦作废=AF 模式无实时显示）** |
| E2 | E1 + 缩放 ×12 叠加 | 若 C2 弱正信号为真则与其协同 | `EA9-E2.bin`（VER 17.2.0，diff 10B，nibble 62）— ○免测（E1 平+x12 已判噪，双无效叠加） |
| H1 | **诚实上报·对照**：镜像字面量 @0x6178 `0x20000494→0x20000368` ⇒ 0x06 帧位置读 **0x2000036E=账本×10 实际行程**（而非目标瞬移值），门仍关（值有陈旧） | 机制实锤（re_af_engine）：环上报位置从来就是目标瞬移、电机真行程从未流过帧 | `EA9-H1.bin`（VER 20.1.0，diff 7B，nibble 8D，base=V3）— ✖**实测：不稳定、合焦更难=陈旧位置"不动"被机身惩罚 ⇒ 全项目第一个位置链正向消费证据（机身要看"动"，不问幅值；勿作日常，§11.3ag）** |
| H2 | H1 + E1 门开放（槽随调保鲜） | 完整真值轨迹上报 | `EA9-H2.bin`（VER 20.2.0，diff 9B，nibble 8E）— ○**实测：无显著差异（恢复 V3 水平=救回 H1 的 stale 问题；"动"足够、滑行无增益 ⇒ 归档，§11.3ag）** |
| I1 | 瞳孔放大率抄数：norm05 W1=body[38..43]←`18 00 A0 00 21 50` | （作者口径字面偏移） | `EA9-I1.bin`（VER 21.1.0，nibble 21）— ✖**死机级（无法对焦/不能释放快门）：W1 含协议标志 b3B=01 被毁，W1 永久禁入（§11.3ag）** |
| I2 | 同抄数灌 W2=body[2E..33]（"1.5 写死区"） | 偏移二选一 | `EA9-I2.bin`（VER 21.2.0，nibble 22）— ○**安全但无差异（VX 该区与 EA9 原值几乎相同，改变量≈0=没测到东西；W2 岛证明可写）** |
| I3 | W1←作者渐变 `F0 EC E4 DC D4 C8` | （照抄作者参数） | `EA9-I3.bin`（VER 21.3.0，nibble 23）— ✖死机级（同 I1，勿再刷） |
| I4 | W1+W2 双灌 | （提高命中率） | `EA9-I4.bin`（VER 21.4.0，nibble 24）— ✖死机级（含 W1，勿再刷） |
| I5 | **II 轮·安全区真改变量**：b2E `18→14`（1.5→1.25 @/16） | 瞳孔初值主探针（W2 安全岛内） | `EA9-I5.bin`（VER 21.5.0，diff 7B，nibble 25，base=V3）— ○实测：略弱（b2E 轴负向，暂弃） |
| I6 | b2E `18→12`（1.125 @/16，超作者梯度下限） | 方向/幅度斜率探针 | `EA9-I6.bin`（VER 21.6.0，diff 7B，nibble 26）— ○实测：好于 I5、与 S3/V3 基线无显著差异（非单调=b2E 弃） |
| I8 | b2E=14+b33=18+b34=48（多点小梯度） | 组合步 | `EA9-I8.bin`（VER 21.8.0，nibble 28）— ○实测：比 I5 略好、远不及 I7 ⇒ b33/b34 近中性，**b30 为决定性字节** |
| I7 | **★命中档★ b30 `A0→80`**（u16 1.25→1.0 @/128） | 瞳孔第二采样点 | `EA9-I7.bin`（VER 21.7.0，diff 8B，nibble 27）— ✔**实测：更好且合焦明显加快=全项目首个 M40 正命中（§11.3ah）** |
| J1 | **b30 峰值搜索**：`80→70`（112，比命中点更深） | 定峰·深侧 | `EA9-J1.bin`（VER 22.1.0，diff 8B，nibble 29，base=V3）— ○实测：不如 I7 稳定（§11.3ai） |
| J2 | b30=`60`（96，深界探针） | 峰值/崩坏边界 | `EA9-J2.bin`（VER 22.2.0，diff 8B，nibble 2A）— ○实测：略好于 J1、不及 I7（§11.3ai） |
| J3 | b30=`90`（144，浅侧控制） | 曲线形状/单调性 | `EA9-J3.bin`（VER 22.3.0，diff 6B，nibble 2B）— ○实测：≈J2、不及 I7 ⇒ **b30 峰=128 坐实** |
| J4 | b30=`80`+b33=`18`+b34=`48`（命中+邻字节梯度） | "五采样点"整体形态 | `EA9-J4.bin`（VER 22.4.0，diff 10B，nibble 2C）— ○实测：好于其余、≈I7、"也许更好"（K 系拆解归因中） |
| K4 | b30=`88`（136，右肩精修） | 定峰 | `EA9-K4.bin`（VER 23.4.0，diff 8B，nibble 30）— ○实测：**似乎略强于 I7 ⇒ 峰或右移 128~144**（L 系夹逼） |
| K1/K2 | （拆解）80+b33 18 / 80+b34 48 | J4 归因 | `EA9-K1/K2.bin`（VER 23.1/23.2，nibble 2D/2E）— ○实测：K1 弱（**b33 有害**）/ K2 正常（b34 中性）⇒ J4 组合效应存疑 |
| K3 | b30=`78`（120，左肩） | 定峰 | `EA9-K3.bin`（VER 23.3.0，nibble 2F）— ○实测：弱 ⇒ 左肩关闭 |
| L3 | b30=`88`+b34=`48`（136+中性协同） | 组合探针 | `EA9-L3.bin`（VER 24.3.0，nibble 33）— ○实测：=K4（b34 协同确认无效）|
| L1/L2 | b30=132/140 | 追峰夹逼 | `EA9-L1/L2.bin`（VER 24.1/24.2，nibble 31/32）— ○实测：皆略弱于 K4 ⇒ **b30 峰=136 定档候选**（§11.3ak） |
| M4 | M2 + b30=88 | 逆序+峰 | `EA9-M4.bin`（VER 25.4.0，diff 13B，nibble 37）— ○实测：≈K4、似乎更好（印象级，§11.3al） |
| M1/M2/M3 | （作者区灌数三组） | 单变量/方向/叠加 | `EA9-M1/M2/M3.bin`（VER 25.1~25.3，nibble 34/35/36）— ○实测：**逆序>正序为真**（M2>M1、M4>M3 双证）但全区对 K4 无稳定净增益；M3≈M2（正序+136 互相抵消） |
| N1 | **收尾探针**：逆序但 b38 留空 0x00（b30=160） | "组起点在 ID 后"排布变体 | `EA9-N1.bin`（VER 26.1.0，diff 11B，nibble 38，base=V3）— ★现役 |
| N2 | K4 + b38=`CF`（MSB=1） | **选择位存在性探针**：照片角部明暗异常=机制存在 | `EA9-N2.bin`（VER 26.2.0，diff 8B，nibble 39）— ★现役·测完回 K4 |
| **K4★日常** | b30=`88`(136)（真身=pl[48] 光圈描述符活字段，§11.3am） | **40mm 日常固件定档（2026-09-05，§11.3al）**——L/M 两代确认无稳定超越者 | `EA9-K4.bin`（VER 23.4.0）— **★ 日常档，生效清单默认** |
| N1/N2 | （作者区排布变体/MSB 探针） | — | `EA9-N1/N2.bin`（VER 26.1/26.2，nibble 38/39）— ○**作废勿测（§11.3am 索引制度正：N 系打在光圈描述符上，非光学行）** |
| O1 | **★真光学行首灌★** slotA(pl32..37=hex b20..25)←selp1650 网格 `a0 ec 6d 49 22 10` | 作者库格式解码验证+网格错配假说（EA9 现抄 Voigtländer15 网格） | `EA9-O1.bin`（VER 27.1.0，diff 12B，nibble 3A，base=V3）— ★现役·后排 |
| O2 | slotB(hex b26..2B)←selp1650 T0 `14 0a e0 fe 18 04` | 瞳孔放大率正形行（EA9 现行向边缘递增=方向反）+模板→帧拷贝链验证 | `EA9-O2.bin`（VER 27.2.0，diff 13B，nibble 3B）— ★现役 |
| O3 | slotA(selp1650)+slotB(T0) 双行 | 全真值组合 | `EA9-O3.bin`（VER 27.3.0，diff 18B，nibble 3C）— ★现役 |
| O4 | slotB←T1 `e1 b9 12 a6 e1 f6`（C/F 0.027 行） | 瞳孔尺寸行（type1） | `EA9-O4.bin`（VER 27.4.0，diff 13B，nibble 3D）— ★现役 |
| O5 | **slotA←Loxia21 网格 `b0 fe 45 47 39 27`（作者表原值）+ slotB←T0** | **最佳组合主攻：21mm FF 网格最贴 M40 + 真瞳孔行** | `EA9-O5.bin`（VER 27.5.0，diff 19B，nibble 3E）— ★现役·主攻 |
| Q1 | **★光圈家族整族一致改写★** pl[44]=`16`/pl[46]=`0E`/pl[51]=`16`/pl[52]=`46`（声明最大光圈 F1.83） | 第 0 步只看机身显示 F 值 | `EA9-Q1.bin`（VER 29.1.0，diff 11B，nibble 43，base=V3）— ✖**实测：显示仍 F2.0 ⇒ 这四个字节被开机例程运行期覆写，模板值不上链（§11.3ap）** |
| Q2 | 家族 X=`10`（声明 F1.41） | — | `EA9-Q2.bin`（VER 29.2.0，diff 11B，nibble 44）— ○**作废勿刷（同 Q1 通道）** |
| Q3 | 家族 X=`28`（声明 F4.00 反证控制组） | — | `EA9-Q3.bin`（VER 29.3.0，diff 9B，nibble 45）— ○**作废勿刷（同 Q1 通道）** |
| Q4 | 家族 X=`16` + pl[48]=`88` | — | `EA9-Q4.bin`（VER 29.4.0，diff 12B，nibble 46）— ○**降级：家族部分无效，等价于 K4，待 Q6 定 pl[48] 是否透传** |
| Q5 | 单点 pl[46]→`16`（归因探针） | — | `EA9-Q5.bin`（VER 29.5.0，diff 7B，nibble 47）— ○**作废勿刷（Q1 已含 pl[46] 改动仍无变化）** |
| **Q6** | **活体探针** pl[48]→`00`（其余 V3 原样） | 判 pl[48] 是否模板透传 | `EA9-Q6.bin`（VER 29.6.0，diff 8B，nibble 48，base=V3）— ○**实测：对焦无差别；但版本显示 "48"=我们的 nibble ⇒ init 帧确实透传（首个硬验证通道）。0 值恐触发"0=用默认"兜底=探针设计失误 ⇒ 重做 Q7/Q8（§11.3aq）** |
| Q7 | **非零离谱探针** u16 pl[48..49]=`02 00`（/128=0.016） | 排除"0=默认值"兜底的活体重测 | `EA9-Q7.bin`（VER 29.7.0，diff 8B，nibble 49）— ★现役（排在 F1 之后） |
| Q8 | 反方向 u16 pl[48..49]=`FF FF`（/128=512） | **Q7+Q8 皆平 ⇒ pl[48] 不上链 ⇒ I7/K4 判 placebo** | `EA9-Q8.bin`（VER 29.8.0，diff 8B，nibble 4A）— ★现役 |
| **Q9** | **★硬读数 canary★** norm05 焦距双拷贝 pl[24..27] 400→**137**（13.7mm，任何预设档都造不出这个数） | **判"norm05 载荷到底是不是我们 flash 模板的透传"**：机身显示 13.7/14 ⇒ 是（静态层活，光圈/光学行的零值=逐字段运行期写，可做定点代码 patch）；仍显示 40 ⇒ 该字段由代码填 ⇒ norm05 静态改动不可信（I~N 判决降级为未证实）→ 转代码层 | `EA9-Q9.bin`（VER 29.9.0，diff 10B，nibble 4B，base=V3）— ★**现役·第 1 步（不测 AF，只看焦距数字）** |
| ~~Q(v1)~~ | ~~pl[51]→`30`(F5.66"诚实化") 四支~~ | ~~谎报错配假说~~ | ○**作废（§11.3ao：M40 真身 F1.8+ 非 F5.6，且环不读实体光圈环；同名文件已被 v2 覆盖）** |
| F1 | **焦距声明偏移**：norm05 pl[24..25]/pl[26..27] 双拷贝 400→**280**（报 28mm）+ 探针 | ~~判定窗缩放假说~~ → ★**新用途：静态层存亡 canary——只看机身设置界面焦距数字（28 或 40），不测 AF（§11.3aq④）** | `EA9-F1.bin`（VER 18.1.0，diff 9B，nibble 63，base=V3）— ○焦距轴本身已关闭（§11.3aa），但**★现役·第 1 步硬读数** |
| F2 | 同上 400→**700**（报 70mm，反方向对照） | （同上，已判） | `EA9-F2.bin`（VER 18.2.0，diff 10B，nibble 64）— ○搁置（同 F1） |
| G1 | **绝对焦点位置回填**：norm06 body[26..27]=**702**（=110.85×√40，M40@∞ 数学正解）+ 探针 | dpreview RE+仓内 5 捕获复核：真镜头 0x06 第三焦点字段全体非零（绝对焦点坐标系），**LM-EA9=0 系全场唯一不填者，且 60+ 历代 patch 从未碰过**（0x6134 镜像不覆写此字段=静态干净）；中心相位 AF 不需它/边缘纯反差 AF 需要=直指"中心绿边缘不绿" | `EA9-G1.bin`（VER 19.1.0，diff 9B，nibble 8A，base=V3）— ○**实测：稳定性略降（弱负=值被消费且静态值与实拍不符？或噪声，G3 判之；MF 辅助金丝雀作废→改 EXIF FocusDistance，§11.3ac）** |
| G2 | 同上=**715**（M40@1m 场景值） | 值敏感性（机身若校验 fp-场景一致性） | `EA9-G2.bin`（VER 19.2.0，diff 9B，nibble 8B）— ○**免测（G3 荒谬值尚无效应，区间内值必无信号，§11.3ad）** |
| G3 | 同上=**354**（0x0162，老 Techart EF 环历史硬码值） | 值敏感度最锐利探针 | `EA9-G3.bin`（VER 19.3.0，diff 8B，nibble 8C）— ✖**实测：无明显变化 ⇒ 机身绿框终检不读此字段幅值 ⇒ G1 微降判噪，G 轴关闭（§11.3ad）** |

*文件名中的 P5 对应 init08-native 实验；以本表为准。

机身侧零成本对照（建议在刷写前先跑）：
- MF+放大对焦确认该位置图像确实锐（区分光学 vs 逻辑）；
- 柔性单点、关人脸/人眼 AF、关"AF 优先实时取景释放"等组合；
- A7R3 若无"AF 微调"菜单则跳过该项（此菜单在 A7R III 系上可能不存在）；
- §8.3 的 v1.6 对比度模式判决实验（只需官方固件来回刷，零风险）。

## 11. 下一步（按依赖序）

1. ~~写 checksum 破译脚本~~ **完成**（规则见 §3，脚本 `cksum_scan.ps1`/`verify_lex.ps1`）。
2. ~~生成 patched.bin 系列 + 刷写套件~~ **完成**（`patches\` 6 个 bin 全部自校验 PASS；`flash_kit\` 含 LST/serve/hosts 脚本与 HOWTO）。
3. 实测进度（第一轮：全版本回测）：P1/P6/P7 ❌ ⇒ 光圈显示路线整体关闭（表内候选全排除，显示源在表外；判据废弃）；P5 ✖ 首按 AF 通信丢失 ⇒ init08 整帧替换关闭；**P3 ★阳性（唯一改变行为：稍近处合焦成功率略升、极限远不变）⇒ §7C"能力声明/确认路径"升为头号嫌疑**。`mk_patch.ps1` 自检同步升级：init34@0x4CDC(len32，官方 cksum 已陈旧 0x01BC≠0x01B6) 纳入帧表并设 0x4CDC~0x4D30 保护区字节一致性断言；判定规则改为"只惩罚 patch 新引入的 cksum 破坏"（orig-stale 帧标 `orig-stale` 不计 FAIL）。
3b. 实测进度（第二轮：P9~P12 二分，测试条件 1~1.5m）：P9 ✖ ≈原版 ⇒ **P4 量程对齐为负项**；P10 ✖ ≈原版 ⇒ @1 中性；P11 ✖ 全场最差 ⇒ **@2@3 为有害字节**；P12 ★ ≈ P3 ⇒ **P3 有效组锁定 {@5,@6,@7}**（P3 = 有效组 + 有害组的叠加，故 P12 以 3 字节达到 P3 全 6 字节的效果）。全部版本在该距离都无法对上极限边缘对焦点。
3c. 实测进度（第三轮：P13~P16 单字节二分）：P16 ★ 优于 P3 ⇒ 去毒正确；**P15 ★★ 优于 P16 ⇒ @7（50→00）就是关键字节，P15 成为新冠军/新基准**；P13 略差于 P16 但**最边缘处更稳** ⇒ @5 是唯一的边缘正信号；P14 明显劣于 P16 但优于原装 ⇒ @6 弱正且冗余；叠加多于必要字节反而稀释（P15 > P16 > P13/P14）。当前代 = **P17（@5+@7，优先）/ P18（@6+@7）/ P19（@1+@7）/ P20（@7=28 中间值·值语义探针）**，探针版本读回 2.9~3.2，生成自检 ALL=PASS（diff 10~11B）。
3d. 实测进度（第四轮：P17~P20 二阶搜索）：**P19(@1+@7) ★★ 成功率≈P17 但合焦更快、总体最好 ⇒ @1 与 @7 存在协同**；P17(@5+@7) ★ 好于 P15 且极限边缘有概率对上；P18(@6+@7) 无增益；P20(@7=28) 劣于 P15 ⇒ **@7 为连续标定值、越低越好，00 是表内端点**。至此 init01 八字节全部定性：@7=00 关键、@5=40 边缘、@1=97 提速协同、@2@3 有毒、@6 稀释、@0/@4 同值。当前代 = **P21（@1+@5+@7 合并双冠军·优先）/ P22（@7=FF 越端探针）/ P23（@5=80 上限探针）/ P24（@1=17 甜点位探针）**，探针版本读回 3.3~3.6，生成自检 ALL=PASS（diff 11~12B）。
3e. 实测进度（第五轮：P21~P24 组合与越端）：P21(@1+@5+@7) 不如 P19 且**偶发走太远脱框** ⇒ @1 与 @5(40档) 相冲、"合并双冠军"证伪；P22(@7=FF) 与 P19 体感类似 ⇒ **@7 的 FF 与 00 等效**（回绕相邻低端皆可，28 才是不稳中间态）；**P23(@5=80+@7=00) ★★★ 又准又快 ⇒ 新冠军**，@5 越高越好且未到头；P24(@1=17) 快但合焦不稳 ⇒ @1=97 甜点位定案。当前代 = **P25（@5=A0）/ P26（@5=C0）一轮夹逼 @5 最优档 + P27（@1+@5=80，高 @5 下 @1 复判）+ P28（@5=80 单独，@7 必要性对照）**，探针版本读回 3.7~4.0，生成自检 ALL=PASS（diff 10~12B）。
3f. **外网检索定案（§14）：init01 body 公开无文档 ⇒ P 系列实验知识即前沿**。@5 夹逼收敛后若仍不满意，静态表新渠道队列（均有 ≥2 真实样本背书、TechArt 为少数派）：**init08 标志位候选字节 [13]/[17]（native=Sigma 同值 E1/15，TechArt 独异 20/40）⇒ init07 四点差异（@0/@1/@2/@8-9）⇒ norm05 距离表段照抄 native ⇒ norm06 焦点位置字段（byte2/3，勿碰已证伪的 @9）**。
3g. **镜头更换事件（2026-08-31）**：P23（3.5.0）在 **FD 35mm 上获显著改善**（首个"显著"级战果，@5 爬档路线成立），但换 **Leica-M 40mm** 后改善消失。⇒ 环对镜头无感知（M 口无触点、上报全恒定表），@5/@7 属"标定档案"性质，**最优字节组随镜头而变**是预期内行为；对策=①在 40mm 上重跑 @5 夹逼（P25~P28 直接可用）②生成 **X 世代 8 个候选**（`_extract\mk_patch_x.ps1`，base=P23，含 Sigma 整行/X4-X5 init08[13]/[17]=§14.4 队列头名/X6 norm05 独异字段清零/X8 F144 复验；探针读回 5.1~5.8，自检 5 帧全 OK）。**日常固件策略改为"每镜头一档"**（不同 LST 一键切换，刷写成本 ~1 分钟）。
3h. **M40 第一轮判决（X1~X4，2026-08-31）**：X1（Sigma 整行）❌无法对焦；X2（P23+@2@3Sigma）❌劣于 P23；X3（P23+@4=82）❌无法对焦；X4（P23+init08[13]=E1）❌无法对焦。⇒ **核心定律升级：TechArt 的"独异值"分两类——@5/@7 是病灶（改了有效），@4/@2@3/init08[13] 是环硬件标定（改了即死，与真样本不同≠错）**。"照抄真镜头/native 值"策略在 init01@4、init08 通道上整体证伪；X5 因含 [13]=E1 连坐停用（[17] 由 X11 隔离重测）。§14.4 队列头名 init08[13] 关闭。M40 剩余安全自由度 = **@5、@7、@6、@1 四轴 + norm05/init07**；新增 X9（@7=7E）/X10（@6=10）/X11（init08[17] 单独）补齐网格。
3i. **M40 第二轮判决（X9~X11，2026-08-31）**：X9(@7=7E)≈P23 ⇒ @7 轴在 M40 平坦（35mm 的单调性不迁移）；**X10(@6=10) 略好于 P23 ⇒ @6 是本镜头至今唯一正信号，@6=10 升为 M40 新基准**；X11(init08[17]=15)≈P23 ⇒ **init08 通道零增益，全线关闭**。第三轮 = **X12~X15**：@6=10 基准上重跑 @5 爬档（A0/C0/40）+ @6=18 上端，另 X7/P25/P26（旧基准 @5 单变量）仍可穿插。若 @5×@6 网格全平 ⇒ M40 的答案不在 init01，转 norm05/init07 温和字段或接受"40mm 用原厂"结论。
3j. **M40 第三轮判决 & init01 收敛（X12~X15，2026-08-31）**：X12(@5=A0)✖、X13(@5=C0)✖✖（越高越差）、X14(@5=40)✖（略好于 X12）、X15(@6=18)≈P23 ⇒ **@5 响应曲线在 M40 上以 80 为峰、两侧单调下降**（与 35mm"越高越好"完全相反），@6 在 08~18 是平坡。**init01 静态网格宣告收敛：M40 最优 ≈ P23/X10/X15 一簇，全部"无明显改善"⇒ 继续改 init01 字节已无空间，此线封盘。** 用户同时反馈：说明书操作（含设焦距）早已做全、F51/F57 无体感差异 ⇒ §15.1#1 焦距设错假设证伪。
3k. **新战线（同日启动）**：①**Y 世代**（norm05 距离表段 + init07 换 native，两帧从未测过，见 §10）；②**固件代码逆向**——已确认可行：app 链接基址 **0x6000**（向量表 SP=0x20002ab0、Reset=0x6145、真实 ISR @0x7505/0x7651/0x7DF9/0x86CD…），Thumb 指令，capstone 反汇编已通（`_extract\re_probe.py`/`re_probe2.py`）；帧表无直接 32 位指针 ⇒ 帧表经 PC 相对/基址寄存器寻址，下一步做函数恢复+找帧发送例程与距离计算逻辑（把"猜表"升级为"读逻辑"，也才能回答"哪些模板字节运行时被覆写"）；③路线 A（HID 审讯）仍是运行时观测首选。工具链：python 3.13.2 + capstone 5.0.7 已装。
3l. **Y 世代判决（2026-08-31，M40）——本轮最重大进展**：**Y1 有效**（对焦变快，幅度不大）⇒ norm05 距离表段是**活字段**，且是 M40 上第一个真实正向增益；**Y2 剧变**：机身**相位检测区域框大幅缩小**、对焦点可移出框外、全画面极易绿框但**实际虚焦（假合焦）**⇒ ①init07 同样是**活帧**，且**直接控制机身 AF 区域绘制与合焦判定松紧**——这是全项目找到的离"绿框判定"最近的杠杆；②native 的 init07 值方向错误（过松致假合焦，不可用作日常）。**推论：AF 区形状/大小是环声明的（非机身写死），"更容易真合焦"的声明组合存在。** Z 世代跟进：Z0=Y1+@6=10 叠加两个正信号；Z1/Z2/Z3 把 Y2 的 init07 八字节拆成三组（@2 单字节 / @9..A 量程对 / @0@1@5@7 标志组），复现"区框缩小"者为控制字节，随后在其邻域细扫找"不假合焦但易合焦"的组合。
3m. **Z 世代判决（2026-08-31，M40）——init07 拆包大成功**：**Z1**（@2 08→60）：框与官方一致、较好梯队、边缘略好、速度快 ⇒ **@2 是安全正信号（速度/行程）、不控框**；**Z2**（@9A EA00→30C7）：框同、边缘极难合焦 ⇒ 量程对方向错误、负效果；**Z3**（@0@1@5@7 标志组）：框很小、比 Y2 更接近合焦但仍虚、**"镜头还在动就绿框/先绿框后到位/似无反差对焦验证"** ⇒ **合焦判定流程的开关就在 @0/@1/@5/@7 四字节中**——这是用户核心诉求（"合焦判定成功、可接受精度下降"）的直接旋钮；Z0（Y1+@6=10）≈Z2 边缘难合焦 ⇒ 该叠加失败。**下一步（Z4~Z7，已生成）**：把四字节逐个拆单，@7 尤其可疑（0x10→00，恰似"反差验证使能"位）。若 @7 单改即复现"先绿框后到位"，则验证开关锁定，之后在 @7 值域（00~10）扫"能真合焦的最松档"并叠加 @2=60 的提速。
3n. **Z4~Z7 单字节拆解判决（2026-08-31，M40）——真合焦突破口**：**Z4(@0 02→01)**：框小、提前绿框、虚焦 ⇒ **@0=合焦判定/反差确认位**（01=跳过确认直接绿→虚；官方 02=严格确认→M40 上永不绿框=原始症状根源）；**Z5(@1 03→01)**：框正常、**反差拉风箱收敛、会真合焦**、慢、边缘难、不提前绿 ⇒ **@1=反差收敛模式位，01 档是全程第一个"能真合焦"的档**（官方 03 在 M40 上反差确认永不通过→永不绿）；**Z6(@5)/Z7(@7)**：框正常、≈Z1 挺不错 ⇒ 非关键安全位。**新认知**：官方 init07 的 @0=02+@1=03 组合 = "严格反差确认且永不通过"，正是 M40 不出绿框的机制根因；@1=01 让机身真正驱动反差收敛到焦，但慢——**W 世代 = Z5 反差收敛 + 提速件**（W1=+@2=60，W2=+norm05 距离表 Y1，W3=双提速），W4/W5 探 @1=02、@0=00 值域边界。若 W1~W3 显著改善，"40mm 用 M40 档"即告成功。
3o. **W 世代判决修正 & 目标重定向（2026-08-31，M40）**：用户澄清 **"会真合焦"仅指中心，边缘所有固件都难合焦**（Z5 的 @1=01 未解决边缘）⇒ **主攻目标从"合焦判定"转为"边缘也出绿框"**。norm05 距离表段（Y1）是唯一"全像面"杠杆（距离↔位置刻度影响全场），**V 世代 = Y1 × 位置刻度对齐**（V1=Y1 单测、V2=+量程 5205、V3=+271→272、V4=+@6=10、V5=+@2=60、V6=全对齐）；W4/W5 值域探测仍待测但降优先。判据统一改为"**边缘是否也绿框**"。
3p. **冠军对比轮 + V 世代判决（2026-08-31，M40 边缘判据，全部对 ORIG 直比）**：用户按统一条件回测历史冠军与 V 世代：**Z1/Z5/P23 ≈ 原版（无法感知差异）** ⇒ 35mm 时代的 init01/@2 增益与 init07@1=01 在 M40 边缘判据下全部出局；**Y1"似乎快点但不稳"**、**V1（=Y1 单独）不如原版** ⇒ 距离表单独用为负项；**X10（@6=10）与 V3（表+272）比原版边缘合焦更快 ⇒ 当前唯二正信号**；**V2/V6 不如原版 ⇒ 量程 5205 确认为毒**（与 Z2 的 init07 量程对失败互证，"量程改大=毒"两代成立）；**P19 不如原版** ⇒ 35mm 结论不迁移 M40。**U 世代（已生成，VER 11.1~11.3）**隔离 V3 活性成分并叠加正号：**U1=272 单独（无表）/ U2=@6=10+272 / U3=表+272+@6=10 顶配**，探针镜头名 U1~U3、init07@6 nibble B1~B3。若 U1 ≈ 原版 ⇒ 272 必须与表联动生效（表"不稳"的锅在缺对齐）；若 U1 好 ⇒ 弃表取 272，距离表路线关闭。
3q. **U 世代判决（2026-08-31，M40）——三因子收官、V3 定冠、开 T 世代**：**U1(272 单独)≈原版** ⇒ 272 自身惰性，V3 正信号=**距离表本体+272 联动纠偏**（表单独负、表+272 正）；**U2(@6+272 无表)<原版** ⇒ @6 与 272 无表时反向联动（@6 单独正、272 单独平、合体毒）；**U3(表+272+@6)≈V3** ⇒ @6 不加分 ⇒ **V3=静态表冠军**。{表,272,@6} 八格仅剩 V4(表+@6) 未读。**T 世代（已生成，VER 12.1~12.3，base=V3）**攻最后未动帧 norm06 焦点位置 body[2..3]（现恒 `00 10`=0x1000，与镜头真实位置脱钩；dpreview 实证初代 Techart 报 0x0162 被机身接受）：**T1=+0x0162 / T2=+0x0000 / T3=+0x1300(=4864 与量程声明一致)**。附带勘误：norm06 整帧 len=0x30=48B（§4 旧记 39B 为 body 长）、版本变异字节实在 body[10]。若 T 全平 ⇒ 静态表收官（日常档=V3、备选 X10），转路线 B 抓 pogo UART 看机身如何消费位置字段。
3r. **T 世代判决（2026-08-31，M40 只测边缘）——norm06 位置字段证实为活杠杆**：**T1(0x0162) 边缘显著弱于 V3**（活杠杆实锤：机身真消费该字段）；**T2(0) 弱于 V3 但好于 T1**（曲线非单调，354 是谷底）；**T3(0x1300=4864) ≈V3 略慢** ⇒ 高位平台 [4096,4864]，**V3 原生 0x1000 已在平台顶附近**，静态微调空间见底。测试协议精简：**只测边缘、中心不再报**。**S 世代（已生成，VER 13.1~13.3，base=V3，唯一变量=norm06 位置）**：S1=0x1455（5205=native 距离表原生刻度端，"表按 5205 设计"假说外推）/ S2=0x1800（6144 远超声明量程，测机身 clamp）/ S3=0x0800（2048 低位补点）。**若 S1、S2 均 ≾V3 ⇒ 静态表收官、日常档 V3、转路线 B**（看真镜头在机身 0x0104 命令流下 norm06 位置如何实时变化，评估"动态位置"是否值得改运行时）。
3s. **S 世代判决（2026-08-31，M40 只测边缘）——静态表正式收官，V3 = 终冠**：**S1(5205) 不如 V3 ⇒ "距离表按 5205 刻度设计"假说证伪**；**S2(6144) 低于 V3 但明显好于 S1 ⇒ 机身不 clamp 超量程，但曲线非单调（5205 是反常低谷）**；**S3(2048) ≈V3 ⇒ 平台左延至 2048**。至此 norm06 位置曲线采齐 {0,354,2048,4096,4864,5205,6144} 七点：2048~6144 大平台皆 ≈V3、仅 354/5205 反常下凹 ⇒ **非单调到这种程度说明剩余差异已落入感知噪声带，norm06 静态榨干**。**结论：init01/init07/init08/norm05/norm06 五帧全部翻遍，无任何静态改法能超越 V3(表+272+位置4096)；"边缘稳定绿框"是运行时层问题（真镜头位置随 AF 实时变化，环只能报静态值），非静态表可解**。日常档 = **V3**（备选 X10）。**下一步转路线 B**：逻辑分析仪抓 pogo UART（§14.1 已证协议同一性），看真镜头在机身 0x0104 AF 命令流下 norm06 位置如何逐帧变化，判"动态位置回填"是否值得改环运行时固件（高风险、需反汇编 STM32）。
3t. **第 0 步（离线，零硬件）——机身侧 C++ 仿真源码 + Sigma 真 0x06 报文，推翻"静态收官"、重开 norm06（详见 §16）**：在 Tapendra 论文同仓库发现作者用 Teensy 写的**"假机身"完整实现**（`docs_ref\camera_sim_backend\`）+ 注释里的 **Sigma 真 0x05/0x06 报文**。两大发现：① **机身每次移动命令 `0x04 DEL = target − 当前`，"当前"直接取镜头自报的 0x06 位置**（`RegularMessagingState.cpp`）⇒ 静态假位置使机身每一步都算错移动量，这是"边缘永不绿"的机理性解释；② 真 0x06 里位置是**双拷贝**（body[2..3] 与 body[20..21] 相同）、body[0]=02 ⇒ **我们 V3/T/S 历代只改 body[2..3]、body[20..21] 一直停在模板值 4096，所有位置实验都背着"双拷贝分歧"这个隐藏变量**。重读八/九轮：V3(两拷贝都 4096，一致)最好、分歧越大越差 ⇒ "对齐假说"统一解释数据，**norm06 并未榨干**。**R 世代（已生成，VER 14.1~14.3，base=S3）**：R1=body[20..21]→2048 消除分歧 / R2=body[0]→02（Sigma flags）/ R3=R1+R2（完整 Sigma 形状）。若 R1≫S3 ⇒ 对齐是关键、下代两拷贝同改重扫；R 全 ≈S3 ⇒ 才真正转路线 B。附带：sigrok Viltrox dump 因 srzip-v2 连续 deflate + 疑似抓到 SPI 建立段（位周期不对、像 SPI 形态），已解析器 `D:\tmp_web\se_parse.py` 挂起，被 §16.1 源码信息替代。**（后记：该解析器实际已攻下 srzip-v2=无头连续 raw-deflate 流并导出 26.8M 样本，唯 UART 解码未出 F0 帧——捕获形态疑为链路建立/时钟门控段，位周期≈13 样本@6MHz；重开时按 CS 高电平窗+边沿采样二次解码。）（2026-09-04 再勘误：解码失败的真正原因是通道位映射错（RXD=bit3 而非 probe7/8），格式与"连续流"结论亦系误判——见 §16.4 勘误与 §17，现已全部解码。）**
3u. **R 世代判决（2026-08-31，M40 只测边缘）——norm06 双拷贝真收官；复活 W4/W5 开打确认位值域**：**R1(双拷贝对齐@2048)≈S3、R2(body[0]=02 flags)≈S3、R3(全 Sigma 形状)"隐隐约约更稳"属噪声级** ⇒ §16.2 对齐假说不兑现正收益，norm06 的既有分歧只解释"大分歧有害（T1/T2），小分歧无感"，**norm06 至此连同双拷贝彻底榨干**；同效簇终判并入用户正在做的 V3/U3/T3/S3/R3 集中对比（当前证据全部 ≈）。**静态最后一条未测轴 = init07 @0/@1 确认位值域**（Z 世代已证 @0=合焦确认模式：02 严格=边缘永不绿根因、01 跳过=提前绿但虚；@1=收敛模式：03 官方、01 中心真合焦）——**W4(@1=02 中间档)/W5(@0=00 比官方更极端)当时生成后从未实测**，2026-08-31 复活（mk_patch_w.ps1 重跑，base=P23，W1~W3 冗余片已删）。**用户"接受精度下降"使 W5 成为直指边缘绿的头号探针**：若 @0=00 让边缘出绿（哪怕稍虚）⇒ 确认机制旋钮定档，下代在 @0∈{00}×@1∈{01,02} 组合里找"绿且尽量真"的甜点；若 W4/W5 均无感 ⇒ 静态全收官，坚定转路线 B。
3v. **W4/W5 实测判决（2026-08-31，M40）——静态表正式收官（终局）**：**W5(@0=00) 机身无法识别接环**（链路握手失败、不认镜头）⇒ **@0 并非"更松的确认旋钮"而是能力位/最低有效声明，官方 02 已是该位下界，任何"比官方更松"的 @0 路线（含 00）彻底关闭**；**W4(@1=02) 无显著提升** ⇒ @1 值域三档扫尽（01 中心收敛慢/02 平/03 永不绿），无边缘解。至此 **init01/init07/init08/norm05/norm06 五帧全部字节空间穷举完毕**，含 §16 双拷贝/真 Sigma flags/确认位值域等全部隐藏变量——**没有任何静态改法能让 M40 边缘出绿框；"边缘稳定绿框"被三重机制锁定：①0x04 DEL 以镜头自报位置为基准做减法（§16.1），静态位置=每步算错；②init07@0=02 严格反差终检在 M40 边缘永不通过（官方即此值）；③@0 无"更松"合法值（00=握手失败）。** 静态工作全部结束。**日常档落定**：35mm = P23（已显著改善）；40mm = 从 ≈V3 同效簇（V3/U3/T3/S3/R3）中选 V3（正信号最充分、版本居中可辨识）。**下一步唯一通道 = 路线 B**（逻辑分析仪抓 pogo UART，看真镜头在 0x0104 AF 命令流下 norm06 位置逐帧变化 → 评估改环运行时"动态位置回填"的可行性；这是超越静态表、让边缘出绿的唯一剩余杠杆）。
   - **（2026-09-04 更新）**：sigrok 解析器已复活+全部解码成功（§17），且固件本体反汇编证实"无位置传感器、上报=开环行程账本"（§17.2/17.3）——通道扩展为"路线 B 自抓真值"或"运行时 RE 深挖账本换算常数（§17.4，若常数在表内则静态实验翻案）"。
3w. **C 世代判决（2026-09-05，M40 只测边缘）——首批代码段 patch 成功落地，缩放轴成唯一活杠杆**：**C0(×10 等价重写) 与 V3 完全一致** ⇒ 代码段 patch 通道无暗坑（diff 10B 级别、flag 副作用、寄存器踩踏全部排除），**运行时层首次获得可信实验能力**——这是自 checksum 破解以来最重要的方法学节点；**C1(×8) 弱一点、C2(×12) 边缘似乎强一点、C3(×13) 无明显差异** ⇒ 四点曲线 8↓/10基/12↑/13平：**缩放常数确实是活杠杆（机身真消费账本×N 的上报值，与 §17.5 机理链完全自洽）**，但 C2 增益幅度"似乎"级且 ×13 即回落——要么峰极窄（±1），要么属感知噪声（T/S 时代"非单调平台+孤立下凹"的前车之鉴）。**D 世代（已生成，VER 16.1~16.4，`_extract\mk_patch_d.py`，base=V3，唯一变量=缩放 N）**：D1=×11（斜率加密·真伪判别主探针）/ D2=×14 / D3=×16（2 的幂奇袭）/ D4=×20（剂量上限）。**判读规则**：① ×11 若介于 10~12 呈缓坡且 ×14 不衰 ⇒ 真趋势，峰值锁定 12~16 区间，下代加密定峰定档；② ×11≈×10 且 D2~D4 全平 ⇒ C2 判噪声，**缩放轴关闭 ⇒ 进"伪造轨迹上报"路线（§17.5 可行性②，真值基线已入仓）**；③ 任何档若"绿框变多"须同时验证**绿后真合焦**（MF 放大确认），排除假绿。若本轮满意可提前定档：M40 日常档 V3 → C2（×12）。
3x. **D 世代判决 + 焦距链意外发现（2026-09-05，M40）**：**D1(×11) 不如 C2（真焦非虚绿）、D2(×14)/D3(×16)/D4(×20) 全部 ≈V3** ⇒ 九点曲线 8↓/10基/11↓/12?/13平/14平/16平/20平：×12 系两侧夹回落的孤立尖峰 = 教科书噪声形态 ⇒ **按 §11.3w 规则 ② 缩放轴正式关闭**（代码层第一杠杆榨尽；机身对标定斜率误差不敏感 ⇒ 边缘问题在"对话节奏/轨迹形态"层而非"数值标定"层）。**意外收获（用户观察）**：机身 F6.3 焦距档显示随 N 线性漂移（44/56/64/80mm=40×N/10 精确）⇒ **焦距上报与位置共享 fn@0x6D2C ×N 链**（该函数真容=零参门控刷新例程，四槽同值写，见 §17.5 更新）；同时获得**免抓包读值窗口**（机身显示=环上报换算值的直接读出）。~~焦距声明对 AF 无影响~~ **（此推论已收回，2026-09-05 用户纠正：测 D 时观察到位移后均已手动把焦距档重新设回 40mm 附近，"80mm 显示"并未持续存在于测试过程；且用户过往实证"焦距偏差过大对合焦有影响"⇒ 焦距声明是活变量，见 3z 之 F 世代）**。M40 日常档维持 V3。**下一步（先分析后写码）**：深挖仓内 5 个 sigrok 捕获的 **TXD 命令流**（机身何时读位置/一轮 AF 问询几次/0x04 移动命令节奏）→ 判定伪造轨迹最小插桩点；若证明"位置每轮只被读一次"则伪造轨迹判死、全力转路线 B。
3y. **对话结构复盘 + E 世代"轨迹门开放"（2026-09-05，`_extract\sig_conv.py`/`sig_rounds.py`）**：对仓内 5 个捕获做双通道合流时间轴分析，三大事实：① **机身-镜头稳态对话 ≈60Hz**（一轮 = B>L 03(29B, 含 w0 变动目标) + B>L 04(22B 移动命令) + L>B 05(105B cur/tgt) + L>B 06(48B 状态)，一轮 ~16.7ms，Viltrox 3.7s 内 205 轮）⇒ **位置消费窗口大开，机身每轮都重读**——"伪造轨迹"路线的可行性前提成立；② **真镜头 05.cur 有滑行波形**：ef50 捕获 327→378ms 段 cur=5824→5824→5568→4544(=tgt)，d(cur-tgt)=1280→1024→0 **逐帧收敛**，而 LM-EA9 是"冻结模板→电机完成一次性跳到 cur==tgt"（§17.5 四槽同值实锤）——真/环在机身眼里的形态差异从未被直接对照，今天有了；③ SEL55210 不响应 0x05（只走 06），Viltrox/LM-EA9 走 05+06 双帧——LM-EA9 与 Viltrox 同构度再+1。**RE 补链（`re_slots.py`）**：06 帧位置源=0x20000494+6（0x6CC0 AF 引擎返回的目标位，经 0x6134 镜像写帧）；0x6D2C 账本×10 输出=0x2000036E/70 双槽（=[ptr678]+30/32 同一物理地址）**疑即 05 帧 cur/tgt 源**——两链分开，C/D 世代改 ×N 能生效证明 05 路径被机身消费（与 §16.1 DEL 基准吻合）。**E 世代（已生成）**：fn@0x6D2C 的"入口检门+刷完自清"一性门形态强烈暗示**调用者是周期性调用**（周期调用才需内部一次性门防重）⇒ **E1=0x6D32 `beq→nop`（2 字节）**：门常开、每次被调都刷新 ⇒ 若调用=60Hz，运动期间账本相位计数直接流入 05.cur = **释放真实行程轨迹**（伪装的"假轨迹"实为真轨迹）；E2=E1+×12 叠加（收 C2 尾巴）。判读：E1 行为变化（含 F6.3 焦距显示跳动=金丝雀）⇒ 调用周期性成立、轨迹上线，后续在"轨迹形态"精修；E1≈V3 ⇒ 调用非周期，回 RE 找调用点（b.w/blx 网扫）或转插 stub 强制周期刷；焦距显示乱跳不止 ⇒ 立刻回滚 V3 复盘。
3z. **用户纠正 ⇒ 焦距声明轴复活，F 世代（2026-09-05）**：用户澄清两点：① 测 C/D 时见到焦距显示漂移后**都手动把档位重新设回 40mm 附近**（=D 世代测试态焦距≈恒定，位置缩放实验不受污染、关闭判决保留，但"焦距无影响"推论作废）；② **过往实测：焦距偏差过大对合焦有影响**。⇒ 焦距声明是独立活变量，且存在一个对症假说：**机身合焦确认窗随焦距声明缩放（报短⇒窗变宽⇒边缘易绿，报长⇒反之）**——与用户"接受精度下降"诉求精确匹配。RE（`re_focal_table.py`/`dump_frames.py`）：全 bin 焦距族 LE16 无系统表；**norm05 body+0x18/0x1A = 400/400 双拷贝（40.0mm，与 06 位置同构的双拷贝形态），且经 ×N 链**（400×N/100=用户所见 44/56/64/80 精确吻合）——候选声明源。**历史包袱披露**：P1（第一代 patch）曾把这对 400 改 144 测"光圈显示"未变 ⇒ 当时判"光圈源在表外"没错，但**没看过焦距显示**，400=焦距声明(40.0mm) or 最大光圈(F4.0) 语义未定。**F 世代（已生成，base=V3，双拷贝同改）**：F1=400→280（报 28mm，判定窗放宽假设）/ F2=400→700（报 70mm，反方向对照）。**三重金丝雀判读**：F1 落地看机身显示——①焦距显示变 28 ⇒ 400=焦距源，F 轴成立，看 AF 行为定方向；②光圈显示变了 ⇒ 400=光圈声明（P1 漏看），焦距另寻；③都不变 ⇒ 该槽未被运行期消费（stale 表），F 轴关闭、焦距真源在档位小表（待定位）。
3aa. **焦距轴判决——用户零成本实测定案，F 轴关闭（2026-09-05）**：用户无须固件即用机身"光圈设焦距"直改环上报值，M40 边缘测试 **12 / 40 / 75mm 无显著差异** ⇒ ①**焦距声明对边缘合焦无杠杆作用**（6 倍范围效应≈0，比 3z 假设的"判定窗缩放"更强的否定）；②F1/F2（表 patch 报 28/70）与该手动通道同输入同语义 ⇒ **搁置**（bin 保留，测试价值已被穷尽；另注意若 400 只是模板，机身设档会覆写 RAM 副本，F 系 patch 本就可能被覆写无效——双保险判死）；③上一轮"焦距偏差过大影响合焦"记忆与本测不矛盾的可能性=当时观察对象非边缘绿（中心/测光/其它镜头），不再追。**现役队列收回=E1→E2 两支，"轨迹节奏"（E 世代门开放）=代码层唯一剩余主轴**；E1 若平 ⇒ 伪造轨迹形态精修（真值曲线发生器）或路线 B。C/D 九点缩放判决不受影响（焦距无关性反过来加固"缩放轴关闭"）。
3ab. **外部咨询定调"边缘=AF confidence 阈值问题" ⇒ 检索战果=第三焦点字段，G 世代（2026-09-05）**：用户经咨询+实验确认主攻方向=如何让机身降低"合焦确认"门槛。文献检索（dpreview E-mount reverse engineering 帖 #53/#55，作者 Entropy512=我们所用 sigrok 捕获之源头；+Metabones 官方"PDAF via metadata translation"声明+Techart 官方 FAQ 自认"AF performance on the edge may not be perfect"）挖出**决定性字段**：0x06 帧 **body[26..27]（帖子 1-based bytes 31/32）= 第三个焦点字段"绝对焦点位置"**，真镜头公式≈`110.85×√fp_mm`（fp=像距，∞时=焦距，越近越大，1/fp=1/fl−1/物距）。**仓内 5 捕获复核（`sig_abs3.py`）：SEL55210@55=0x0319、@210=0x0694、SELP1650@16=0x01FE、Viltrox EF50=0x02B3、EFS24=0x03C8——四支真镜头全部填写；LM-EA9 模板=0x0000 全场唯一不填者**，且此字段**历代 60+ patch 从未碰过**（0x6134 运行期镜像只覆写 b[2..3]/[20..21]，b[26..27] 静态模板原样存活=patch 干净靶点）。Entropy 当年关联观察：该字段错误 ⇒ **MF 放大辅助不触发、机身距离标尺不显示、长焦端 AF 变差**——与"机身缺绝对坐标系无法终检合焦"机制吻合；且**中心相位点不需要它、边缘纯对比度 AF 需要**=恰好解释"中心能绿、边缘永不绿"。老 Techart EF 环曾硬填 0x0162(=354≈"等效 10.2mm"错值) 但"至少非零"。**G 世代（已生成，base=V3，norm06 body[26..27] 单点，VER 19.1~19.3，nibble 8A~8C）**：G1=702（M40@∞ 数学正解）/ G2=715（M40@1m）/ G3=354（Techart 历史硬码值，测"任意非零即可"假说）。金丝雀=机身是否开始显示对焦距离标尺/MF  assist 是否出现（=字段被消费的落地证明），判据=M40 边缘绿框。**若 G 成功 ⇒ 下一步把 absv 动态化（随行程账本计算 fp 回填）=完整"绝对位置回填"方案**。现役队列=**G1→G2→G3→E1→E2**（G 优先：公开 RE 背书+机制直指 confidence+零历史覆盖）。
3ac. **G1 判决 + 金丝雀通道修正（2026-09-05）**：**G1(702=∞正解) 实测"稳定性略有下降"**（弱负信号，两读法：①字段真被消费、静态值与实拍距离不符⇒干扰终检——**"值敏感"反证**；②噪声）。**MF 放大辅助/距离标尺金丝雀作废**——索尼 MF 辅助触发条件=镜头端转动对焦环，转接环无对焦环、物理不可触发（我方设计盲区，用户指出）。**~~替代金丝雀=EXIF FocusDistance~~ 亦作废（2026-09-05 实测三张样张：原厂55mm/V3/G1，`exif_dump.py`+`exif_diff.py`）**：A7R3 对**任何镜头（含原厂 FE 55/1.8）都不写 tag 0x000D/SubjectDistance**，MakerNote 为无 mini-TIFF 的私有二进制（37672B）；不同场景的两张 MN 字节 diff 被拍摄内容淹没、无鉴别力。**EXIF 观测通道就此关闭**。副产物确认：A405/A432/A434 焦距字段=环上报（40 vs 55 正确区分）。**G 轴判定回归纯 AF 行为，方法论升级=同场景 A/B 交替计数**（V3 与 Gn 交替各 15 次边缘合焦，同一固定目标+三脚架，消除记忆/场景偏差）。**队列调序 G3(354)先行**：它是值敏感度最锐利探针——若 354（错一个数量级）明显更差 ⇒ 消费+幅值校验成立，G1 微降为真 ⇒ 值得做动态回填；若 354≈V3 ⇒ G1 微降判噪、G 轴降温回 E。G2(715) 观察区间内值敏感性。
3ad. **G3 判决（2026-09-05，同场景标准法）——G 轴关闭，静态绝对位置出局**：**G3=354（错一个数量级的荒谬值）无明显变化** ⇒ 三点合读 {0=V3 基线, 702=微降(印象级), 354=平}：若机身在边缘绿框终检中消费此字段，幅值错 2 倍必显著劣化——无劣化 ⇒ **A7R3 的反差 AF 判定不读该字段（或至少对幅值不敏感）**，G1"微降"按预登记规则判噪声；且"非零本身有益"亦不成立（354 非零=平）。**G2(715) 失去判别价值，免测。G 轴（静态绝对位置回填）正式关闭**；Entropy512 的 MF 辅助/长焦退化关联观察与本案和解=他测 A6000 且观察对象是 MF 链路，非 A7R3 边缘绿框。**剩余假设树收敛**：静态表(关)→缩放(关)→焦距声明(关)→静态绝对位置(关) ⇒ **现役回 E 世代：E1(0x6D32 beq->nop 轨迹门开放) -> E2(+x12)**；E1 亦平 ⇒ 代码层节奏轴也出局，剩"真值轨迹形态发生器（H 世代大改）"与"路线 B 硬件抓包"两级。E1 金丝雀提醒：D 世代已证机身显示=上报换算值直读，若门开放后周期刷新成立且账本在动，**焦距显示可能出现随对焦动作的瞬时漂移/跳动**——有跳=刷新链路活，无跳且行为平=调用非周期。
3ae. **E1 判决 + AF 引擎反汇编大发现 ⇒ H 世代"诚实上报"（2026-09-05）**：**E1 对焦能力无显著差异**；且用户补充关键 UI 事实：**A7R3 正常 AF 模式下根本没有实时焦距/对焦距离显示**（显示只在设置界面静态呈现）——焦距显示金丝雀通道作废（第三个工作日设计盲区，记录：**该机身可观测通道只有：设置界面的焦距值、相位框形状、绿框本身**）。E1 平按预登记=门控刷新的调用频率不是解药。**但 `re_af_engine.py` 反汇编挖出全项目最重磅机制**：主循环 0x6F92 调 AF 引擎得 r0=**目标位置** → 0x6F94 `strh r0,[0x20000494+6]` → 0x6134 镜像入 0x06 帧 ⇒ **环上报给机身的"位置"从头到尾就是目标瞬移值——电机真实行程（账本）从未流过 0x06 帧**；帧的镜像值源由单一引用字面量 @0x6178(=0x20000494) 决定 ⇒ **改 4 字节字面量为 0x20000368，`ldrh[r3,#6]` 即落到 0x2000036E=账本×10=真实行程槽**——无状态、无 stub、无插值的"诚实上报"。**H 世代**：H2(主攻)=E1 门开+字面量重定向（槽随调刷新+帧读实际值=完整真值轨迹上报）；H1(对照)=仅重定向（门仍关，值会陈旧——若 H1≈V3 而 H2 有变化，则刷新频率×真值双因子皆必需）。若 H2 仍平 ⇒ 位置上报内容轴（值/标定/节奏/真伪）四轮全灭，**正式定性 A7R3 边缘绿框与镜头位置上报无关**，剩余=①路线 B 抓包看机身到底发了什么（可能根本不是位置问题而是反差采样时序）②光学极限（像素间距×M40 边缘慧差=信号本身不可用，Techart 官方"edge may not be perfect"的真实含义）。现役队列=**H2 -> H1**。
3af. **原帖作者第4/5篇更新 ⇒ "瞳孔放大率"新大陆，I 世代（2026-09-05，用户主动提供）**：作者（有我们不具备的**真 AF 镜头固件**做参照）宣布核心突破：**"手拧对上焦点机身也不认"根因=光学补偿参数未传**——M 头边缘光线入射角斜，机身 PDAF 需要"瞳孔放大率"校正（长焦更垂直所以表现更好=其镜头依赖解释）。该参数在 **0x05 消息 byte38..43**：u16 初值+4×1B 增量=中心到边缘 5 采样点，取值 0~2，**向边缘/向远递减**；EA9 写死 1.5,0,0,0,0；他抄真镜头数据改设 1.24→1.16 后**对焦区域 30%→66%**（未随距离动，还嫌不稳）。另两情报：①"EA9 给机身过去值+现在值，镜头厂给现在值+未来预期值"——与我们 06 帧"目标瞬移/账本真值"发现(H系)同一块地；②EA9 视频不能 AF=疑不返回马达状态(移动中/完成)——新独立问题。**与我们的覆盖对照**：马达/位置语义=已挖穿(H 待测)；**光学补偿数据=完全未试**（我们的"native"参照是接圈模板 lex_constants.h，不是真 AF 镜头；05 内 b2E..33 区从未动过；**28/35 消息从未测**——0x28 在 EA9 帧表存在、0x35 作者称 EA9 未实现）。**偏移校准**（`sig_pupil.py` 对 5 捕获实测）：Viltrox 05 该结构在 **body b2E..33**=`18 00|a0 00|00|16/21|50`（b2E=24→**1.5@/16 与作者"EA9写死1.5"对上**；b34=0x50=1.25@/64 与 b3E 01→03 为"随远递减"候选变化位）；SEL 系 PZ 镜头该区全 0；作者说的"38-43"可能系整帧计数(=body 32..37)或 body 计数(=38..43=我们的近邻区)⇒ **双窗口 W1=body[38..43](V3原值几乎全0=即作者"EA9传0") 与 W2=body[2E..33](V3原值=18 00 a0 00 00 20 即 1.5 写死区) 都测**。**I 世代（已生成，base=V3）**：I1=W1←Viltrox24 抄数(`18 00 A0 00 21 50`) / I2=W2←同值 / I3=W1←作者渐变(`F0 EC E4 DC D4 C8`=1.24→1.16@/64) / I4=W1+W2 双灌。**新队列=I1 -> I4 -> I3 -> I2 -> H2 -> H1**（作者实证权重>我们推断；I 若成=直接对症边缘绿，H 降级为节奏微创新）。若 I 全平再问作者要编码细节/考虑 28/35 帧补齐。
3ag. **I 轮1 判决 + H 系判决（2026-09-05，重大双面信息）**：①**I1/I3/I4 无法对焦/无法释放快门/机身死机**——三支都覆写了 W1=body[38..43]，其原值 `00 00 00 01 00 00` 中 **b3B=01 被清零=协议标志被毁** ⇒ 机身挂死。**W1 永久禁入**。且与捕获核对后确认：**Viltrox 真镜头的 b38..3D 与 EA9 完全相同（`00 00 00 01 00 00`）——该区两族一致、无瞳孔数据**，作者的"38-43"计数基准与 EA9 布局错位（其"1.5"我们定位于 **b2E=0x18@/16**，偏移差 12）。**I 轮1 教训：区定位靠字节值比对+安全岛渐进，不靠作者口径**。②**I2（VX 抄数灌 W2）安全但无差异**——因 VX 与 EA9 该区原始字节几乎相同（仅 b33 20→21），**改变量≈0，等于没测**。③**H1"感觉不稳定、合焦难一点"= 全项目第一个位置链正向消费证据**：把 06 帧位置换成陈旧账本值(不动) ⇒ 合焦变难 ⇒ **机身看的是位置"动不动"，不看幅值是多少**（完美和解 G3/C/D 平：幅值随便改、只要动就行；V3 的目标瞬移"会动"所以正常）。H2(新鲜账本)≈V3 ⇒ "动"恢复但滑行无增益。**位置轴定性升级：活的、但只认运动性**——④**II 轮（安全区真改变量，已生成）**：I5=b2E 18→14(1.5→1.25@/16) / I6=b2E→12(1.125) / I7=b30 A0→80(1.25→1.0@/128) / I8=b2E14+b33 18+b34 48(多点梯度)——单字节级、区安全已证、改变量真实。⑤**最高优先行动=向作者提问**（一条评论即可定死）：05 消息 38-43 是"整帧含 F0 还是 body"起算+编码单位(/16?/64?)+五个值的用途顺序(中心→边缘?)；拿到答案前 II 轮只是包围战术。现役队列=**I5 -> I6 -> I7 -> I8**（H 系已判：H1 禁用作日常、H2 无增益归档）。
3ah. **★I 轮2 判决：I7 命中——瞳孔轴第一个实质正信号（2026-09-05）**：**I7（b30 `A0→80`，u16 160→128 即 @/128:1.25→1.0）= "更好且合焦明显加快"**——全项目（自 checksum 破解以来 20+ 代实验）第一个在 M40 上被用户确认"明显加快"的 patch，方向与幅度皆与作者理论吻合（0-2 区间、向低改=增益）。对照组：I5(b2E→1.25)略弱 / I6(b2E→1.125)好于 I5 但≈基线 = **b2E 轴非单调、不清晰，暂弃**；I8(b2E+b33+b34 组合、无 b30)只比 I5 好一点点点、远不及 I7 ⇒ **b30 是决定性字节**（I8 的 b33/b34 改动近乎中性）。曲线现状：{160=基线, 128=明显好}，峰值未知。**J 世代（b30 峰值搜索，已生成）**：J1=`70`(112 更深) / J2=`60`(96 深界探针) / J3=`90`(144 浅侧控制, 描曲线形状定单调性) / J4=`80`+b33`18`+b34`48`（命中值+邻字节梯度组合，测"五采样点"整体形态）。**J 之后预案**：①峰值定档 ⇒ 该档并入日常固件候选（**40mm 日常档即将换代**）；②作者真值随距离递减，我们是静态模板 ⇒ 若静态峰值仍不够，下一手=**运行期距离渐变表**（05 帧组装处代码 patch——位置链"运动性"证据(H1)+瞳跳轴已通, 动态化有动机也有靶点）；③355/28 消息补齐仍是未开垦地。**另：用户已删仓库根部 TECHART_Updater(USB-WIN).exe（实测无法识别接环，仅 kit 内另一更新程序可用）**。现役队列=**J1 -> J2 -> J3 -> J4**。
3ai. **J 世代判决：b30 峰定 128、J4 或可再压 I7 ⇒ K 世代（2026-09-05）**：J1(112)不如 I7 稳 / J2(96)略好于 J1 但仍不及 I7 / J3(144)≈J2 / **J4(128+b33:18+b34:48) 好于其余、≈I7、也许更好**。判读：①**b30 轴峰=128 坐实**（两侧 112/144 皆回落；J2>J1 的深侧非单调疑为噪声级）；②J4 的"也许"必须拆解归因 ⇒ **K 世代**：K1=80+b33`18`（去 b34）/ K2=80+b34`48`（去 b33）/ K3=b30`78`(120 左肩) / K4=b30`88`(136 右肩)。**决策树**：K1 或 K2 ≥ J4 ⇒ 命中元素确认；K3/K4 任一 > I7 ⇒ 峰在 120~136 微调；**全部 ≤ I7 ⇒ I7 定峰定档=升 40mm 日常固件**（改 serve 默认清单+文档"当前最优=I7/VER 21.7.0"），动态表为下一站。**当前排名（M40）**：J4≈I7 > {J2≈J3} > J1 > 基线 V3。现役队列=**K1 -> K2 -> K3 -> K4**。
3aj. **K 世代判决 + L 世代追峰 + 向作者发评求口径（2026-09-05）**：K1(80+b33 18)=弱 ⇒ **b33 改动有害**；K2(80+b34 48)=正常 ⇒ b34 中性 ⇒ **J4 的"也许"非来自邻字节，组合本身存疑（可能噪声）**；K3(120)=弱；**K4(136)=似乎略强 ⇒ 峰可能右移至 128~144**。**L 世代（追峰，已生成）**：L1=`84`(132) / L2=`8C`(140) / L3=`88`+b34`48`(136+中性协同探针)。判读：L1/L2 夹出真峰即定档；L 全 ≤ I7/K4 ⇒ 承认 128~136 皆平台、取中或按用户偏好定档（届时**升 40mm 日常固件**动作执行）。**并行行动=向原作者发评**（文案存 `致原作者_评论草稿.txt`，含 GitHub 地址）：亮我方独有成果（无位置传感器+账本×10 全链反汇编/06 帧=目标瞬移/H1"机身只认运动性"证据/EA9 上 body[38..43] 实为协议标志区改动死机/瞳孔决定字节实测在 body[0x30] 附近 u16@/128 128~136 峰/离线刷写通道+checksum Σ 规则/sigrok 通道映射勘误），问死三项：①38-43 精确口径（含不含 F0 头、0/1 起）②编码单位与五值顺序 ③28/35 消息帧布局（尤其"EA9 未实现 35"的判据来源）。作者若回口径 ⇒ 动态表与 28/35 补齐一步到位。现役队列=**L1 -> L2 -> L3**。
3ak. **L 定峰 + 评论区挖出作者亲口编码结构 ⇒ M 世代（2026-09-05）**：①**L 判决**：L1(132)/L2(140) 略弱于 K4、L3(136+48)=K4 ⇒ **b30 峰=136(0x88) 夹逼完成，K4 为定档候选**（动态表前最优=K4）。②**评论区核弹级情报**（作者与懂哥"深圳牛马科技"对线，全部免费）：**05 的 38-43 六个字节里放两组数据会轮换，选择位=首字节 bit7/MSB：1=暗角补偿、0=出瞳**（作者亲口）；懂哥："说的是 3B 里面的 ED 数据吗"（作者：不是，我说 38-43）→"**暗角不轮换，出瞳数据才轮换**"→"**半画幅 6 字节、全画幅 9 字节**"。③**我方 I1 死机真相水落石出**：I1 写 `18 00 A0 00 21 50` 到 b38..3D 时 **b3B 的 01 被清零 + b3A 被写 0xA0(MSB=1=把选择位翻成暗角组)** 双重违规——b3B/b3E 两个 01 是**组 ID/结构字节**，正是死机元凶（此前"协议标志"猜测方向对、细节错）。④**由此导出 EA9 布局自洽假说**：五瞳孔值放 **b38,b39,b3A,b3C,b3D 五个空位**，b3B=01、b3E=01 为两个子组 ID 不可动；编码 **@/64**——作者五值 1.24/1.22/1.20/1.18/1.16 = 79/78/77/76/74 = 0x4F/4E/4D/4C/4A **全部 <0x80 ⇒ MSB 自动=0=出瞳组**，与"MSB 选择"完美自洽。⑤**M 世代（已生成，脚本内置双 assert：ID 保留+全字节 MSB=0）**：M1=五值正序纯灌(b30 保持 160=单变量) / M2=逆序 / **M3=正序+K4 峰 136（最大赌注）** / M4=逆序+136。⑥**懂哥"全幅 9 字节"提示后续**：若 M 成功但作者级(30%->66%)未满，扩到 9B 形态（b38..40? 待作者/懂哥口径）是明确的再下一代。**队列=M1 -> M3 -> M2 -> M4**；若死机重现=假说错，立即重刷 K4 恢复（回滚路径畅通）。同时已备好**追问评论续集**（9B 布局/ID 语义/轮换机制/编码确认，见 致原作者_评论草稿.txt）。
3al. **M 世代判决 + 6B 池结构定样 + K4 升日常档 + N 世代收尾探针（2026-09-05）**：①**M 判决**：M1(正序纯灌)=略弱 / M2(逆序纯灌)=还行但不稳 / M3(正序+136)=≈M2 / **M4(逆序+136)=≈K4、似乎更好**。读法：**逆序>正序是真实方向信号**（两组对照一致：M2>M1、M4>M3 ⇒ 该组字节确被机身读取且 EA9 槽位顺序与作者假设相反，边缘→中心）；但**整区灌数相对 K4 无稳定净增益**（M4"似乎更好"仍是印象级）。②**6B 池结构定样**（V3 模板扫描）：0x3B/0x3E 两个 01 相距 3、**b41 以后全 0 无第三个 ID** ⇒ "数据,数据,ID"周期假说否定；最自洽读法=**两 ID 各辖 2 值位 + 首值 = 4+1=APS-C 五点形态；全幅 9 值的后 4 值在 EA9 模板无槽位可放 ⇒ 6B 池只装得下 5 点，9B 需帧扩容代码 patch（大改，押后）**。③**为何 M 区不及 b30 轴**（待解）：EA9 运行期可能根本不从模板喂这两组（静态死数据）或作者区在 EA9 布局右移错位——b30 恰好命中 K4=136 且随序效应真实存在，**瞳孔效应确认在 b30 不在 b38 组**。④**K4 正式升为 40mm 日常固件**（生效清单默认=K4；M4 需 A/B 计数法连续两轮确认才准换代）。⑤**N 世代收尾探针（已生成）**：N1=逆序但 b38 留空（0x00）——测"首值位无效/组起点在 ID 后"排布变体；N2=K4+b38=0xCF(MSB=1)——**选择位存在性探针：若照片四角明暗出现可见异常 ⇒ MSB 选择位机制在 EA9 上真实存在（顺带白嫖暗角数据通路）**。队列=**N1 -> N2（测完回 K4 日常）**。⑥评论续集待发：问死"EA9 的 b30 是不是出瞳中心值/9B 排布/轮换由谁驱动"。瞳孔轴在静态层已近似榨干，下一梯队=①N 系探针结果 ②作者回复 ③随距离动态表（代码层）④帧扩容 9B（大改）。
3am. **★★作者开源协议库（weiziqian/E-mount-protocol-RE）⇒ 索引制度正 + 真光学行首碰 = 项目认知转折点（2026-09-05）**：作者应我们评论建库共享（optical_data.md + msg_0x05.md 全文已研读，`_extract\sig_rows.py` 复现验证）。**①索引制度正（血泪教训）**：作者 pl 索引=**十进制**，我方 hex b 标注系统性偏 24：**b2E=pl[46]、b30=pl[48]、b38..3D=pl[56..61]——I/M 两代"瞳孔区"其实全打在作者字段表的【光圈描述符 pl[44..59]】+ pl[60] 对焦移动方向 byte 上！** I 系死机真凶=**pl[60] 方向字节（合法 00/01/FF）被写 0x50/0x4A 等非法值**+pl[59]（作者注 UNKNOWN）被毁；M"逆序>正序"双证降级为光圈区噪声（撤回）。**②对作者的反向输出（我们的独有发现）**：pl[48]（作者表 UNKNOWN、全设备定值不活）改 `A0→80/88` = **M40 合焦明显加快**——光圈描述符家族里唯一被我们试出的活字段，已备好回评素材（评论草稿追加段）。**③真光学行格式（作者 CERTAIN 级）**：6B 行=**block-float 五点曲线**——byte0=bit7 型标志(0=T0 瞳孔放大率 p/1=T0=瞳孔尺寸 C/F)+bit6..4 指数 E(3bit 回绕)+bit3..0 尾数高 4，byte1=尾数低 8，byte2..5=**4×有符号 delta**；v0..v4 累加后 ×2^-E_abs；锚点实证：slotA nibble 数值=E_abs(b=11✓)、type1=nibble(e=14/f=15✓Loxia 例吻合)、type0 固定 10（VX/EA9 null 行 `26..`→1.5✓、selp1650 `14..`→1.01✓）。**slotB 逐帧轮换 T0/T1**（0x28/0x35 恒 parity0；EA9/Viltrox 不轮换=机身永远只见到静态一种——作者"只采一种轮换的机身只拿到一半"）。**④EA9 模板真相（`mk_patch_o.py` 打印）**：**slotA(pl32..37)= `b1 28 47 51 4b 38` 与作者表格中 Voigtländer 15/4.5 的行逐字节相同——Techart 抄了一支超广角的网格当所有镜头用**；slotB 模板=`26 08 35 30 40 39` 解码 p=1.51→1.72 **向边缘递增（与真镜头"向边缘递减"方向相反！）**；作者参考 payload 显示 EA9 运行期 slotB=`26 00 00 00 00 00`（运行期组装可能只取模板首字节）⇒ **模板→帧拷贝链待 O 系验证，无响应则 RE 组装点（搜 #0x26 立即数）**。**⑤EA9"冻结缺陷"权威清单（作者字段表×我方反汇编互证，H1"只认运动性"的具体化）**：pl[4] 合焦倒计时=0 死、pl[6..7]=16 永冻（全网独此一家，开机值冻成常数）、pl[8] 运动状态 07 死（真值：待机 3/06、驱动中 2/07 交替）、pl[22] bit6"正在合焦"旗标死 0、pl[23] 物距屈光度永远 FF（=宣称无穷远）、pl[54..55] 剩余行程死 0、pl[60] 方向死 0——**"让它动起来"清单=代码层下一批靶点（Yongnuo 开源固件里每个字段的实现源码可抄）**。**⑥O 世代（真光学行首灌，全部为捕获/作者表格原样字节=格式合法，最坏无响应不该死机）**：O1=selp1650 网格 / O2=selp1650 T0 瞳孔行 / O3=双行 / O4=T1 行 / **O5=Loxia21 网格(作者表原值,最贴 M40 类)+T0 瞳孔行=最佳组合主攻**。队列=**O5 -> O3 -> O2 -> O4 -> O1**；N1/N2 被 O 取代（N 打在光圈描述符上，无效实验）。**当前日常档仍=K4**。⑦未开垦地更新：0x28 消息（EA9 帧表有、同样带 A/B 记录 `[0x11..0x1C]`、O 成功后第二个战场）；pl[4..8/22/23/60] 动态化代码 patch（"动起来"清单）。
3an. **用户行为学情报：机身光圈设定参与 AF 计算（有甜点）⇒ EF 光圈编码实证 ⇒ Q 世代"诚实声明"**（2026-09-05，测试 O 前的补充）：①**用户观察**：光圈>F2 的镜头（1.2/1.4）设 **F2/F1.8 合焦成功率最高**、偏大偏小皆降；**M40 设定 F1.8 显著优于 F2**——机身侧"光圈输入"确为 AF 参数（推翻早期"光圈显示仅曝光用"假设的适用范围）。②**EF 1/8 档编码实证**（F=2^((v−8)/16）：作者表标 pl[51] UNKNOWN，但 **Viltrox EF50（真 F1.8）pl[51]=0x16→解码 F1.83 一锤定音=最大光圈声明位**；EA9 模板 pl[44]=0x20(F2.83)/pl[46]=0x18(F2.0)/pl[51]=0x20(F2.83)——**挂任何镜头都谎称 F2.8（35/2.8 遗产）**，M40 真身 F5.6→v=0x30。③**K1 判读反转**：当时 pl[51] 0x20→0x18（2.8→2.0=往更谎报方向）="有害"⇒ 对称预测：**声明=真实（0x30/F5.66）应变好**。④**双路径理论**：机身设定值=战略输入（用户甜点 F1.8）、环声明值=事实字段（应诚实）——两通道独立，错配三角（真实 5.6/声明 2.8/设定 1.8）决定表现；此理论同时吸收"甜点"与 K1 现象。⑤**Q 世代（已生成，base=V3）**：Q1=pl[51]→0x30（单点诚实化，主攻）/ Q2=pl[44]+pl[46]+pl[51] 三处全 0x30 / Q3=pl[51]→0x36(F7.34 过冲括线) / **Q4=诚实化+pl[48]=88(K4 活字段) 叠加=最终配方候选**。⑥**测试协议更新（重要）**：**机身光圈设定固定 F1.8（M40 甜点）再测 O/Q**；并**追问：以往 C/D/I/J/K/L/M 各轮测试时设定值是多少**——若曾漂移，它是贯穿全程的隐藏噪声变量（部分"似乎/好像"判决可能要重测）。⑦O4 缺陷备案：借用的 selp1650 type1 行≈F4.2 口径，M40/5.6 应有 C/5.6≈0.0178 的形——已向作者追加请求"F5.6 级镜头的 slotB 行"（评论草稿增补）。现役队列=**O5 -> O3 -> O2 -> Q1 -> Q4**（O4/O1/Q2/Q3 候补）。
3ao. **用户两条澄清 ⇒ §11.3an 的 Q v1 前提作废 ⇒ Q v2"光圈家族"+发现免费判读通道（2026-09-08）**：①**LM-EA9 读不到实体光圈环**（用户确认：拧镜头光圈环，传给机身的恒为 F2.0）⇒ **"F1.8 比 F2 好用"是纯光学效应**（真实进光量/边缘光束角度变了），**不是声明通道的功劳**——§11.3an 的"双路径错配"推理对 EA9 不成立；②**M40 镜身标称最大光圈 = F1.8 或更快（1.2/1.4/1.7）** ⇒ Q v1 的"诚实化到 F5.66(0x30)"是**反向谎报**（把快头说成慢头），前提作废；③**全程历史测试恒为 F2.0** ⇒ 隐藏噪声变量担忧解除，I7/K4 等判决内部一致、**不必重测**；④作者 msg_0x05 光圈节选（权威）：`pl[44]=最大光圈`、`pl[46]=pl[44]−8`（恒"快一档"，三个用此字段的设备皆如此）、`pl[48]=0xA0 全设备恒定 UNKNOWN`、`pl[51]=pl[44] 的第二拷贝`、`pl[52]=最小光圈`；模板 0x20/0x18/0x20/0x50 ⇒ 家族自洽（46=44−8、51=44、52=44+0x30）；且 **EA9 开机例程把它从 0x20/0x50 改成 0x18/0x70（F2.0…F90），"a9 II 上显示的就是 F2.0"——与用户观察逐字吻合** ⇒ **模板 pl[44]/pl[46]/pl[51]/pl[52] 可能被开机代码覆写=静态 patch 无效**（而 pl[48] 大概率不被覆写，这正是 K4 能生效的原因——顺带给"pl[48] 其实不属于光圈家族、是被作者划进该区间的另一量"一个新证）；⑤作者"这六个字节清零 ⇒ 机身显示 F1.0 且**完全拒绝 AF**"⇒ 该字段是 AF 的硬门，轴未关闭；⑥**K1 判决的第三种解释（最优）**：K1 单独把 pl[51] 0x20→0x18 = **破坏 pl[51]==pl[44] 不变量**（pl[44] 仍 0x20）⇒ "有害"源于**自相矛盾**而非方向；⑦**Q v2（已重写生成，base=V3，VER 29.x，nibble 43..47）**：一律**整族一致写入**（44=X、46=X−8、51=X、52=X+0x30）：Q1=X=0x16(F1.83=甜点/真身) / Q2=X=0x10(F1.41，比真实更快⇒机身按更斜光束做瞳孔偏移校正，与用户甜点方向一致) / Q3=X=0x28(F4.00，声明更窄=预测更差=**反证控制组**) / Q4=Q1+pl[48]=0x88(K4) / **Q5=只改 pl[46]→0x16（单点归因，看谁喂机身显示）**；⑧★★本轮最大方法论收获=**光圈值是机身实时可见的第二个读数窗口**（此前判"只有设置界面静态焦距"）⇒ **第 0 步不用测 AF**：刷 Q1 后**只看机身显示的 F 值**——显示变 1.8 ⇒ 模板值上链，Q 族有效，继续 AF 判决；显示仍 2.0 ⇒ 开机覆写 ⇒ Q 族静态改动作废，改为 **RE 找开机写点**（搜 0x18/0x70 写入 05 帧缓冲处）做代码 patch。现役队列=**Q1(显示判读) -> O5 -> O3 -> O2 -> Q4**（Q2/Q3/Q5 候补）。
3ap. **Q1 判决：机身显示纹丝不动 = F2.0 ⇒ 05 帧的"光圈家族"是运行期写、不吃模板 ⇒ 顺带拷问 O 世代 ⇒ Q6 活体探针（2026-09-08）**：①**实测**：刷 Q1（pl[44]→0x16/pl[46]→0x0E/pl[51]→0x16/pl[52]→0x46）后机身光圈显示**仍是 F2.0**——我上一轮设的"显示读出口"判据走到"仍 2.0"分支。②**镜像扫描定死原因**（`re_aperture2.py`/`re_aperture3.py`/`re_decl_src.py`）：光圈家族字节串 `20 00 18 00 A0 00 00 20 50` 全镜像**只有一份**（@0x4BAE=norm05 pl[44]，排除"改了没在用的副本"）；而作者实测运行期值 `0x18/0x70`（=F2.0/F90）**在 20172 字节镜像里根本不存在**（搜遍 18↔70 相邻对、u16 组合、movs 成对，全零命中）⇒ **不是拷贝自模板，而是开机例程算出来再写进发送缓冲**（模板 0x20/0x50 → 运行期 0x18/0x70 = "放宽一档/放宽四档"，与作者"a9 II 显示 f/2.0"完全一致）。⇒ **pl[44]/pl[46]/pl[51]/pl[52] 的静态 patch 天生无效**，Q1~Q5 全部作废（Q3/Q5 不必再刷）。③★**同一个逻辑立刻波及 O 世代**：作者抓包 EA9 运行期 **slotA = `00 00 00 00 00 00`（全零）**，可我们 flash 模板里的 slotA 是 `b1 28 47 51 4b 38`（Voigtländer15 网格，非零）——**模板有值、线上是零 ⇒ 光学行同样不吃模板**！⇒ O1~O5 的静态灌行大概率也是空转（这就是 §11.3am 里我标"模板→帧拷贝链待验证"的那一环，如今被 Q1 间接证伪了一半）。④**唯一的反证 = K4**：如果整个 pl[44..59] 区都被运行期覆写，那 K4（改模板 pl[48]=0x88）不该有任何效果——可它报"略强"、I7（同一字节 0x80）报"明显加快"。两种可能：**(A) 开机例程只重写 pl[44]/[46]/[51]/[52] 四个光圈码，pl[48]（作者标 UNKNOWN 的 0xA0）是从模板透传的** ⇒ I7/K4 是真的、这条静态路还活着；**(B) pl[48] 也被重写 ⇒ I7/K4 全是印象噪声 ⇒ 全项目唯一正命中崩塌**。⑤**Q6 = 判定 (A)/(B) 的活体探针**（已生成，VER 29.6.0，nibble 48，diff 8B，base=V3）：**pl[48]→0x00**（退化值；作者实证"这一区清零 ⇒ 机身显示 F1.0 且完全拒绝 AF"）。判读：**明显劣化/不释快门 ⇒ (A) 成立**（模板透传，K4 转正，且光学行区也值得再试一次）；**与 V3 无差别 ⇒ (B)**——静态模板层对 05 帧内容整体失效，I 系到 N 系的"瞳孔轴"结论全部降级为未证实，**下一步只剩代码层**（RE 运行期写点/05 帧组装函数，把 slotA/B 与光圈字段变成我们可控）+ 路线 B 抓包。⑥**向作者追加的关键一问**（评论草稿已更新）：请给一份 **EA9 完整 05 帧载荷的 16 进制原样 dump**（AF 进行中与合焦后各一帧）——对照我们的模板就能逐字节算出"哪些偏移透传、哪些被覆写"，这一份 dump 直接决定静态层还有没有仗打，比任何单支实验都值钱。**现役队列 = Q6（活体判定，一支定生死）-> O5 -> K4（日常）**；Q2/Q3/Q5 作废候补。
3aq. **Q6 判决 + "ver48" 意外白送一个硬验证通道 + 探针设计失误备案（0=默认值陷阱）⇒ F1 焦距 canary（2026-09-08）**：①**"显示 ver48" 是预期的**：48 = 我们给 Q6 写的代际标记（`init07` 载荷 pl[6] 由 0xA0 系改写为 0x48），机身/上位机把它当版本号**按十六进制原样显示**——这是全项目**第一次拿到"模板字节→机身显示"的直接证据**（证明 init 系帧确实透传上链），也暴露一个方法论漏洞：**以前从不核对刷写是否真生效**（若某次没刷进去，我们会把"没变"当成实验结论）。**新规：每支固件刷完先核对版本显示 = 该代际的 nibble**（V3=A3、F1=63、Q6=48、Q7=49、Q8=4A）。②**Q6 对焦无差别**——但这不能直接判 pl[48] 死刑，三种解释仍开着：**(i)** norm05 该区被代码覆写（⇒ I7/K4 是印象噪声）；**(ii)** 值上链但 **0x00 被代码/机身当"0=用默认"**兜底掉（**我的探针设计失误**：拿 0 做退化值恰好落进最常见的 fallback 分支）；**(iii)** 值活着，但"印象级 AF 判据"分辨不出。⇒ 补 **Q7 = u16 pl[48..49]=0x0002**（非零、/128=0.016 离谱小，兜不住默认值）、**Q8 = u16=0xFFFF**（离谱大）；**两支都平 ⇒ (i) 定案**。③**地址学修正**：向量表 Reset=0x6145/NMI=0x6141 确认 **BASE=0x6000**（文件偏移+0x6000=VA，帧模板 norm05 在 VA 0xAB7C）；但**全镜像扫描 0 条 `ldr[pc]` 字面量指向帧模板区**（`re_frame_refs.py`/`re_base.py`/`re_base2.py`）⇒ 帧表不是靠绝对指针访问的（索引计算/`adr`+偏移/或先搬进 RAM），"直接反查 05 帧组装函数"这条路要走不通，改从**已知 TX 锚点**反推：06 帧 RAM 基址 0x20000494 + 镜像函数 0x6134 是实锤，05 帧的 RAM 缓冲与写点应能用同样方法定位（搜 0x20000xxx 区间字面量的引用者）。④★★**换判据：不再测 AF，用"机身上能读出来的数字"判静态层存亡**。首选 **Q9**（VER 29.9.0，nibble 4B）：把 norm05 的焦距双拷贝 pl[24..25]/pl[26..27] 400→**137（=13.7mm）**——**故意选一个任何预设档都造不出来的数**（F1 的 280=28mm 会撞 28mm 预设，判读有歧义，故降级为备用）。而 D 世代已证机身会原样显示环报的非常见焦距值（44/56/64/80mm 都显示过）⇒ 读数可信。判读：**显示 13.7 或 14 ⇒ norm05 载荷=我们 flash 模板的透传**（静态层活着；光圈/光学行的"线上是零"就是逐字段的运行期写，定点代码 patch 有靶）；**仍显示 40 ⇒ 该字段由代码填**（预设路径），静态 norm05 改动整体不可信 ⇒ I/J/K/L/M/N 的效果记录全部降级为未证实、K4 退役、日常档回 V3、转代码层+求 dump。⑤**现役队列 = Q9（第 1 步·硬读数）-> Q7 -> Q8 -> 定案**；每支刷完先核对版本显示 nibble（V3=A3、Q6=48、Q7=49、Q8=4A、Q9=4B），最后回刷 V3（或 F1 结果显示 13.7 则 K4 保留候选资格）。
4. 若 @5 夹逼收敛（80/A0/C0 定案）后综合表现仍不满意 ⇒ 静态表挖掘收官（以当时最优 patch 作日常固件），要达到"极限边缘稳定合焦"须进运行时层（§7E）实锤观测：
   - 路线 A：底座 USBPcap 抓 TECHART 升级程序流量 → 反推 HID 封装 → Python hidapi 伪机身审讯环（查 F51/F57 双档模板应答 diff）；
   - 路线 B：魔改手动 E-E 接圈，pogo 针焊漆包线 + 逻辑分析仪抓通信线（UART，§14.1；机身侧真流量，含 0x0104 AF 驱动命令与环的实时应答）。

## 12. 工具链教训（PS5 / 本工程）

- `> file` / `Out-File` 常丢输出 ⇒ dump 直接走控制台。
- 内联命令里 `$_` 会被吞 ⇒ 一律写 .ps1 用 `-File` 执行，脚本保持纯 ASCII。
- 官方 releasenotes 为 GBK ⇒ 用 `conv_notes.ps1` 转 UTF-8 BOM 再读。
- 函数定义必须先于调用。
- **E-mount 固件表扫描器必须接受 len=0 帧**（以尾部 `0x55` 回退定界），否则漏检 typ1B/28/class00。
- s3mbed（原 hall-hite/s3mbed，开源 EF→E 环，曾作语义参照）**已从 GitHub 消失（404）**，搜索无有效镜像；语义参照仅剩 `lex_constants.h`。字段语义除光圈/长度/校验外均为合理推测，以实验判决为准。
- PS5 `[byte]值 -shl 8` 会**截断回 byte**（移位保留左操作数类型宽度）⇒ 拼 LE16 用 `[int]$b + [int]($h*256)`，别用 `-shl`。本工程曾因此把多帧误判为 MISMATCH。

## 13. 用户历史请求（背景，全部已答复）

1. 解包升级 exe、提取全部固件 URL → 完成。2. 建目录全下载 → 完成。3. 评判小红书原帖猜测方向 + 检索开源项目 → 完成（原帖疑"速度/算法"，本档转向"确认信息面"）。4. 评判横向色散/竖线 PDAF 假说与"只在 F57 改 F1.2"的可行性 → 完成（档位为环本地开关，需先问 F57 到底改了什么）。5. 查明清库如何离线刷改后固件 → 完成（§9）。6. 全局 F1.2 之外的候选修改点 → 完成（四梯队清单→本文档 §7 收敛版）。7. 五版本实测无差异的解读 → 完成（§6）。8. 专用底座下的抓包方案 → 完成（路线 A/B/C）。9. F1.2 之外影响合焦判定的字段 → 完成（§7 分层清单 + §10 矩阵）。10. v1.8 是否开启对比度确认 → 完成（§8：固件层无此开关，看 init01 声明；实验推论）。11. **破解 checksum（用户睡眠期间自主推进）→ 完成**：算法=Σ字节和（§3），native 8/8+TechArt 11/16 双向验证；顺带产出"官方改 body 不重算 cksum⇒机身不校验"法证与 init01 尾部勘误（§7C）；P 系列 6 固件 + flash_kit 刷写套件全部生成并自校验通过（§10）。

## 14. 外网公开文档检索结论（2026-08-31，本机 10809 代理完成）

### 14.1 协议同一性与 init01 定位（证实）

- 三个**互相独立**的来源与 §3 帧结构逐字段吻合（0xF0 头/LE16 长度/class/seq/type/body/LE16 和校验/0x55 尾，UART 8N1、750kbps 协商后升 1.5Mbps）：① dyxum 论坛 Entropy512 长帖；② LexOptical Google Doc《Sony E Mount Lens Protocol》（`docs_ref\lexoptical_google_doc.txt`）；③ Tapendra 2025 论文附录（`docs_ref\emount_paper_tapendra_text.txt`）。⇒ LM-EA9 消息表就是 E-mount 机身↔镜头协议模板。
- **init01 = type 0x01，公开命名 "Capabilities?"**（init 阶段镜头向机身申报能力/标定，SEQ=0）。
- **公开边界**：论文原话 "initialization messages…their body is not of interest"——**init01 body 八字节的逐位含义在全网公开资料中不存在**。我们五轮 P 实验的 @1~@7 定性即检索可见范围内最前沿。官方规范锁在 Sony NDA 内。

### 14.2 init01 body 三样本对照表（本段新立）

| 样本 | @0 | @1 | @2 | @3 | @4 | @5 | @6 | @7 |
|---|---|---|---|---|---|---|---|---|
| TechArt 原厂（@0x4A0C） | FF | 9F | 60 | 7D | 80 | **07** | 08 | 50 |
| native（LexOptical 固件模板） | FF | 97 | 78 | 15 | 80 | 40 | 10 | 00 |
| **Sigma 15/1.4 真镜头（A7R III 实机捕获）** | FF | 9F | 78 | 5D | 82 | 60 | 18 | 7E |

- @0 恒 FF；**@1 Sigma=TechArt=9F**（9F 非乱值，P24 判 17 差的甜点旁证）；**@5 我们已推到 80/A0，超过一切真样本（≤60）** ⇒ P25/P26 若现天花板，@5 疑为编码值而非线性增益；@7 三样本 00/50/7E 无单调 ⇒ 八字节更像按型号打包的标定档案。
- 论文附录含 0x01 双向完整帧（机身请求 body=`FF×9 2F F8 00×19`，Sigma 应答即上表第三行），在 `docs_ref\emount_paper_tapendra_text.txt` 尾部 Table（p.24 附近）。

### 14.3 "PDAF 对比度终检位"：公开资料无此文档化开关

- 已对全部到手资料（Google Doc、论文、开源源码、论坛帖）穷举 contrast/confirm/flag 检索：**唯一成表的名义标志位 = 0x06 消息 byte0 "Limit Flags"**（bit4=Focus Max、bit3=Focus Min），与终检无关。
- LexOptical 镜头侧 Arduino 实现（模板回显即可骗过 NEX7，源码在 `docs_ref\lexoptical_emount_src\`）无任何"终检模式"分支；A7R3 菜单亦无对应开关。⇒ 传闻（含 §13-10 用户旧问）无公开出处；实质可用的操纵面 = **0x08 能力表（201B）的声明内容**。

### 14.4 init01 之外的渠道（三样本首diff，按嫌疑排序）

| 渠道 | 帧/偏移 | TechArt | native | Sigma（论文捕获） | 备注 |
|---|---|---|---|---|---|
| **A. init08 能力大表** | 02/08 @0x4A88（201B） | [0..3]=`00 13 00 19` [7]=16 [13]=20 [17]=40 [20]=28 | `55 14 55 14` [5]=40 [9]=01 **[13]=E1 [17]=15** [20]=00 | `00 11 00 18` [7]=80 [9]=14 **[13]=E1 [17]=15** [20]=14 | [0..3] LE=对焦行程步数（4864/6400、5205、4352/6144）；**[13]/[17] 两真样本一致而 TechArt 独异 ⇒ 头号标志位候选**。P5 只否决过"整表替换"，单字节未试 |
| B. init07 | 02/07 @0x4A38（34B） | `02 03 08 01 00 00 18 10 A0 EA` | `01 01 60 01 00 01 01 00 A0 30` | — | @0/@1/@2/@8-9 四处全不同，疑驱动类型/行程声明；小表低风险 |
| C. norm06 焦点位置 | 01/06 @0x49D4 | 静态模板 | — | 真镜头实时变化 | **dpreview 第4页实锤：初代 Techart 把焦点位置硬编码 0x0162 报机身** ⇒ 机身消费 byte2/3 位置字段；@9 的版本变异（16/60）已被版本考古证伪勿再碰 |
| D. norm05 距离表段 | 01/05 @0x4B7C body[32+] | 全零 | `B1 28 47 51 4B 38 27 08 35 30 40 39` | — | 疑距离编码表（§5 旧记录），照抄 native 零风险 |
- 排除项：Neto-Zeme/emount 仓库为低价值推测笔记（物理层误写为 SPI）；**LensCAP 在 GitHub/GitLab 各拼写变体均零命中，弃线**；sigrok.org 反 AI 爬虫（其内容经 Entropy512/sigrok-dumps GitHub 镜像取得，捕获清单已固化 `docs_ref\sigrok_dumps_sony_emount_readme.txt`）；grep.app JSON API 已失效。

### 14.5 来源与抓取技法

- 来源：[Tapendra 论文 PDF](https://github.com/Tapendra-195/Sigma_Lens_Control/blob/master/docs/E_Mount_Protocol.pdf)（7.5MB 未入库，文本提取已固化）· [LexOptical Google Doc](https://docs.google.com/document/d/1iw54nzrF0bzQgLINpcP9F8Odd0N5cd7LjlwCDPTNZK0/edit) · [dyxum topic119522](http://www.dyxum.com/dforum/emount-electronic-protocol-reverse-engineering_topic119522) · [dpreview 帖第4页](https://www.dpreview.com/forums/threads/e-mount-reverse-engineering.3872069/page-4) · [LexOptical/E-Mount 源码](https://github.com/LexOptical/E-Mount) · [LexOptical/E-Mount-Traffic-Samples](https://github.com/LexOptical/E-Mount-Traffic-Samples) · [Entropy512/sigrok-dumps](https://github.com/Entropy512/sigrok-dumps)。
- 技法：**Google Docs 抓取必须 `curl.exe -x http://127.0.0.1:10809 ".../export?format=txt"`**（WebFetch 必失败）；PDF 文本用 python 3.12 + pypdf（pip 亦走代理）；GitHub 检索用 `api.github.com/search/repositories`（免认证可用）逐个翻 contents，优于已失效的 grep.app；GitLab raw 会被 Cloudflare 挑战页拦截。原始工作文件在 `D:\tmp_web\`（仓外，换机即失，故关键文本已固化入 `docs_ref\`）。

## 15. ★焦距设置机制（2026-08-31 从说明书挖出的被忽略通道，可能颠覆 P/X 系列前提）

重读 `TECHART LM-EA9 产品使用说明书.html` 第 103~111 行 + `焦距VS光圈对应关系表.jpg`，发现 LM-EA9 有一套**"用光圈档位当输入通道来告诉接环当前镜头焦距"的机制**，此前所有固件分析都忽略了它：

- **M 口无电子触点**，接环无法自动获知镜头焦距。天工的解法：用户把**机身光圈拨到表里对应 F 值**（F2.8=12mm、F3.2=15mm…F5.6=35mm、**F6.3=40mm**、F8=50mm、F11=80mm…F40=800mm），**按一次快门激活**；"激活后不受关机影响"⇒ 存于环内 NVRAM。
- 另三个特殊"光圈值"是**功能档**：F45=关机停靠位、F51=常规对焦、F57=慢速对焦（超大光圈头用）。⇒ **§6 悬案"v1.6 删双 profile 后 F57 慢速怎么实现"有了答案：慢速档是运行期由"光圈= F57"这个命令通道切换的内部模式，不在静态帧表里**。
- 说明书原文（第 108 行）：**"更换镜头时，应确保镜头焦距与设定的焦距相匹配，以保证机身正确的防抖和较为良好的对焦体验。"** ⇒ 官方明确：焦距设错 ⇒ 对焦体验直接劣化。
- 正常拍摄时机身光圈须留在 F2.0（曝光基准），与 norm05 body[24]=400=F2.0 恒值吻合 ⇒ **norm05 那个 F2.0 是协议约定、不是 bug，这解释了为何 P1/P2/P7 改光圈字段全无反应**（§10 光圈路线关闭的机理根因）。

### 15.1 对当前实验的解释与立即行动

 1. ~~最可能根因=焦距没设~~ **已被用户证伪（2026-08-31）**：用户表示说明书上的操作全部做过（含按表设焦距），且 **F51/F57 两档实测无体感区别** ⇒ ①焦距通道正常；②F51/F57 差异对本症状无效（或该命令通道在 v1.8 上作用极弱）。§15 的机制知识保留（解释了光圈路线为何无效、F 值输入通道存在），但"设错焦距"不再是候选根因。
 2. ~~零成本判决实验~~ 已被用户日常操作覆盖，无需再做。
 3. **静态表里的焦距痕迹**：init08[20]=0x28=40（此前注"疑 MFD 40mm"）很可能就是**默认焦距=40mm**；norm05 [28..29]=271、量程 4864 等位置参数应与"设定焦距"联动。若 NVRAM 焦距覆盖表内值，则改表无效——需路线 A（伪机身审讯）确认焦距存储位置。
 4. **新 patch 方向（待 #1/#2 结论）**：把 init08[20] 按目标镜头真实焦距设值（如 35→0x23、50→0x32）而非盲改 @5；以及"照抄 native 距离表段 norm05[32+]"（本就是 §14.4 队列、现优先级上调，因其与距离↔位置映射直接相关）。**已实施为 Y1/Y2/Y3（见 §10）；注意实测发现 norm05 body[38]=0x26 非零（"TechArt 全零"说法不精确），Y 世代绕开该活字节只填零槽。**

## 16. 第 0 步成果：机身侧参考实现 + Sigma 真报文 ⇒ 发现"位置双拷贝"隐藏变量（2026-08-31）

### 16.1 机身侧仿真源码（比 sigrok dump 更硬的收获）

在 Tapendra 论文同一仓库（[Tapendra-195/Sigma_Lens_Control](https://github.com/Tapendra-195/Sigma_Lens_Control)）发现其 `backend/` 是**用 Teensy 写的"假机身"完整实现**（作者先抓真 A7R III+Sigma 15/1.4 再照写），关键文件已固化 `docs_ref\camera_sim_backend\`。要点：

- **checksum 规则第三次独立确认**：`sum(buffer[1..len-4])`（`Message::setFooter()`），与我们 §3 完全一致。
- **主循环机身行为**（`RegularMessagingState.cpp`）：每个 BODY_POLL_LENS 脉冲镜头回 0x05+0x06；机身收到 0x06 立即 `currentLensPos = body 位置字段`，**然后发的 0x04 移动命令 `DEL = target − current`（以镜头自报位置为基准做减法！）**。⇒ 环自报的位置直接参与机身 AF 驱动解算，静态假位置=每步 DEL 都基于错误基准。
- **主循环收到 0x05/0x06 以外的任何 type ⇒ 机身复位链路回 idle**（`default: mState=&idle`）⇒ 完美解释 X1/X3/X4"直接丧失对焦能力"（init 应答被改坏→卡死/复位循环）。
- **init 简化流**：0x0B→3F→3D→08→09→0D→10→0A 逐步请求-回显；0x0A 完成进 Regular。仿真未发 0x01/0x07（真机身有，见论文 Fig8/Table13）⇒ init01/07 是"机身请求-镜头应答"的能力/模式表，语义仍 NDA 黑盒（§14 结论不变）。
- **seq 规则确认**：回显应答 lens 回 s+1；0x10 后机身 seq 复位；`mSequenceNumber%0xF0`。

### 16.2 Sigma 真 0x05/0x06 报文藏在该仓库 Constants.h 注释里（原文已随固化件保留）

- 真 0x06（48B 整帧，与我们 norm06 同长！）：body[0]=**02**（limit flags）、body[2..3]=`9E 25`、**body[20..21]=`9E 25`（同一位置的第二拷贝，与论文 Table10 "byte26/27 usually same as byte8/9" 互证）**。
- 真 0x05 是 117B（len=0x75，我们的 norm05=105B 不同款），其 body[38..] 有 `B1 09 42 3F 3A 2C…`——与我们照抄的 native 距离表 `B1 28 47 51…` **同模板家族**（都以 B1 开头）⇒ Y1 抄表方向正确。
- **隐藏变量勘误（重要）**：我们 T/S 世代只改了 body[2..3]，**body[20..21] 一直是模板原值 `00 10`(4096)**。重读八/九轮判决：V3 最好=两处一致(4096,4096)；T3(4864|4096)差 768≈持平略慢；S3(2048|4096)差 2048≈持平；T2(0|4096)差、T1(354|4096)最差——**"双拷贝分歧越大越差"比"位置绝对值"更能解释全部数据**！

### 16.3 R 世代（已生成并已测，VER 14.1~14.3，base=S3，脚本 `_extract\mk_patch_r.ps1`）——判决见 §11.3u

- **R1** = S3 + body[20..21]→`00 08`（双拷贝对齐 2048）— 实测 ≈S3（对齐不兑现正收益）
- **R2** = S3 + body[0]→02（真 Sigma limit flags）— 实测 ≈S3
- **R3** = R1+R2（完整 Sigma 形状）— 实测"隐约更稳"属噪声级 ⇒ **norm06（含双拷贝）真收官**
- 判决后转向：静态最后一条未测轴 = init07 确认位值域 **W4(@1=02)/W5(@0=00)**（已复活，现役）。
- **W4/W5 实测终判（2026-08-31）**：W5(@0=00)=机身无法识别接环（握手失败）⇒ @0 是能力位、02 即下界；W4(@1=02)=无提升 ⇒ **静态表全收官，见 §11.3v**。

### 16.4 sigrok Viltrox dump 状态（暂缓）

srzip=格式 v2：块**无头**、且是**单条连续 raw-deflate 流的切片**（各块独立解压报错、共享字典连续解压成功，26.8M 样本@6MHz≈4.47s）。但观测位周期≈13 样本(461k)且 probe7/8 无名却有规律脉冲——与"750k UART"不符，疑似抓的是 link-establishment/SPI 形态段，需按 CS 门控+时钟采样二次解码才可用。**已被 16.1/16.2 的信息替代，挂起**；解析器留在 `D:\tmp_web\se_parse.py`（工作文件，重启路线 B 时再深挖）。
- **勘误（2026-09-04，见 §17）**：上面"解码失败"的真正原因是**通道位映射错了**——README 里的"7: RXD / 8: TXD"是**卡口引脚号**，对应 sigrok 的 **probe4/probe5**；fx2lafw 打包顺序 probe1=bit0 ⇒ **RXD=bit3、TXD=bit4**。旧解析器按 probe7/8（bit1/0，未命名位）解码，自然"位周期不对、没有 F0 帧"。且 GitHub 镜像分支里这批 .sr 是 **v2=zip+每 60KB 一个独立 deflate 成员**（zipfile 直接可读），并非"共享字典连续流"——那个结论是拿错成员切片解压造成的误判。**§17 已全部解码成功。**

## 17. sigrok 解析器复活 + 固件位置模型反汇编——"有没有位置传感器"的实证回答（2026-09-04）

### 17.1 解析器复活（数据/代码都已入仓，不再怕换机）

- 数据源：`git clone -b emount https://github.com/Entropy512/sigrok-dumps` → 5 个 .sr + README 已固化 `docs_ref\sigrok_sony_emount\`（共 683KB，A6000+Viltrox EF50/EF24、Sony 55-210 双焦段、SELP1650 的 init 捕获）。
- 新解析器（仓内）：`_extract\sig_decode.py`（通用解码+帧抽取）、`_extract\sig_stats.py`（逐字节时间序列统计）。**全部 5 个捕获解出合法帧，checksum 通过率 ≈100%**（如 ef50：RXD 415 帧全对 / TXD 324/331）。解码参数：6MHz，**spb=8（750k，链路初速）与 spb=4（1.5M，协商后速）都要试**，帧窗口按 CS=bit5（LENS_CS）/bit2（BODY_CS）印证。
- **真镜头参照系（首次实锤）**：Sony SEL55210 的 0x06 帧里 `b[8..9]`（论文定义的 LENS_POS）真实随对焦行程连续变化（0x322A→0x3311→…→0x33E8 收敛），`b[13]`（余量）52→35→22→15→11→0F→0C 单调递减到静止——**工作设备就是在逐帧上报活位置**。Viltrox 转接环（同赛道的另一家电动转接环）的 0x05 帧 `b[0..1]` 也逐帧移动（c0 16→c0 15→c0 11 收敛到 `b[2..3]`）：**0x05 b[0..1]=当前位置、b[2..3]=目标位置**，两值不等=正在移动。⇒ LM-EA9 模板里恒等的 4864/4864 正落在同一对槽位上。

### 17.2 LM-EA9 固件本体：位置变量确实存在，且运行期写进 0x06 双拷贝

- 外设字面量穷举（`_extract\re_sensors.py`，T2）：固件只引用 TIM2/TIM3/TIM6/TIM7/TIM16(+USART/FLASH/扩展块)——**全固件 0 处 ADC 引用、无 I2C/SPI 外设引用** ⇒ **不存在任何模拟位置传感（电位器/线性霍尔），也不存在外置数字编码器芯片**。位置只可能来自：电机步进累计（开环）或 TIM2/TIM3 编码器模式（增量式，只数脉冲、无绝对基准）。
- 帧缓冲与镜像（`_extract\pos_trace.py`）：0x06 帧 RAM 缓冲 @**0x20000000**（引用最热的数据块），内部位置变量在状态结构 @0x20000494 的 +6..7。代码 0x6134-0x613C：**`ldrh r2,[0x20000494,#6]` → `strh r2,[0x20000000,#8]` + `strh r2,[0x20000000,#0x1a]`** ——把内部 16 位"位置变量"同时镜像进 0x06 帧的 body[2..3] 和 body[20..21]，**Sigma 双拷贝模式在 LM-EA9 固件里有原生实现**。
- 位置变量的源头（0x9A5C/0x6F8A 区）：0x20000494 实为**状态结构**（+6..7=位置、+0xC=累计量、+0x10=差额）。0x9A68-0x9A72 做 `subs`（累计 − 上次执行值 → 存 +0x10，再把累计值写回）后 `blx` 调驱动例程；0x6F8A-0x6F94 把 [r4+6] 备份到 +0x16、**用一个被调例程的返回值刷新位置变量**（该例程身份待追）。两处都不是传感读数 ⇒ 指向**软件维护的开环推算账本**（步进/行程累计），0x6FB2 附近另有 `ldrsh [r2,#6]→strh [r2,#8]` 的带符号镜像变体。

### 17.3 对用户假说的回答："接环根本没有位置感应器"

**基本成立**：
1. **无绝对位置传感硬件**（0 ADC/0 编码器芯片引用，§17.2）——环对自己镜组的"位置"只有一个**开环推算值**（内部变量 @0x20000494+6，执行行程的软累计）；
2. 该推算值**确实**会被镜像上报进 0x06 双拷贝（0x6134 镜像点、0x6FB2 附近另一写点），所以协议层"动态上报"存在——但上报的是**开环账本**，没有物理校准环节；**开机归零用什么基准、行程→光学位移的换算准不准，决定一切**；
3. ⇒ 这给"边缘永不绿"补上**最底层解释**：机身拿到的位置反馈是环的自估计数；真镜头有标定 ROM+限位校准，环没有对齐"光学真实"的机制，机身 DEL=target−当前 的推算被系统性污染——中心区 PDAF 冗余大能容忍，边缘区置信度低直接放弃；
4. ⇒ **静态表实验连"解释"层面的剩余价值也耗尽**：表值只是开环账本的**初值**，改表=改初值，T/S 平台期=任何初值都一样差。下一步若有动作，只剩两条硬路：**路线 B**（自抓真值，§11.3v）或 **运行时 RE 深挖**（追 0x9A76/0x6F92/0x6F98 的 blx 目标、找到"行程→上报值"的换算常数，评估"劫持 0x06 上报轨迹"的运行时 patch——若账本换算错了，改那个常数可能才是全项目真正的靶心）。

### 17.4 待办（本轮衍生）

- [x] 追 0x9A76、0x6F92、0x6F98 三个 `blx` 目标（账本更新链与发送链）→ 见 §17.5
- [x] 找"行程账本→0x06 位置值"的换算/缩放常数 → 见 §17.5：**代码内嵌 ×10，不在表内 ⇒ 静态实验不翻案（结论封死）**
- [ ] TIM2/TIM3 访问点细读（0x40000010/0x40000020/0x40000080）：编码器模式 or PWM 判定（§17.5 后降级为旁证项：相位输出走 GPIO，TIM 更像 tick/PWM）
- [x] 交接 §2.3 更新（已随 §17 初版写入，§17.5 结论再补）

### 17.5 位置账本全链闭合（2026-09-04 续，运行时 RE 第一轮，脚本 `re_ledger.py`/`re_ledger2.py`/`re_ledger3.py`）

**上报位置的完整产生链**（每一环都有指令级证据）：

1. **电机驱动 = GPIO 开环步进**：fn@0x8D9x 按模式(r5∈{0..3})向 `[[r4+0x18]+0x14/0x16]` 写 **1/2/4/0x80** ——0x14/0x16 正是 **GPIO_ODR/BSRR 偏移**（F0 GPIO 布局 MODER0x00…IDR0x10,ODR0x14,BSRR0x18），小值图样=H 桥相位表 ⇒ **步进相位查表输出，无 IDR 回读、无闭环**。与 LM-EA7 拆解（步进电机+丝杆+导柱平台）一致。
2. **行程记账**：16 位软计数 @**0x20000680**（字面量 0x2000067C 的 +4；xref：命令分发器 0x66C8 区、0x9B18 区、×10 函数 0x6D60）。
3. **换算与上报（靶心所在）**：fn@**0x6D2C**：`ldrh [cnt]` → **`×10` 硬编码**（0x6D42-0x6D48：`lsls#2; adds; lsls#1; sxth` = x+4x=5x, ×2=10x）→ 写入两组 16 位槽：**[[0x20000678 指针]+30 / +32]** 与 **[0x2000036E / 0x20000370]**（指针+30/32 疑似"当前/目标"对——Viltrox 0x05 实测正是同构两槽；与帧的连接方式待二次确认）。门控：`[0x2000051C+0xB]≠0`（电机动作完成后置位）才刷新 ⇒ 静态模板值只在"开机到第一次行程"窗口内被上报，之后位置即由账本×10 接管（量化步长 10）。帧侧实锤独立存在：0x6134 镜像（[state+6]→0x06 帧 body[2..3]+[20..21] 双拷贝）确认**帧位置在运行期被内部变量覆写**。
4. **AF 引擎**：fn@0x6CC0 按命令字节 `{0x1B,0x24,0x17}`(state+0xA) 分派到 0x8E28/0x5A94/0x5C08/0x5B98（行程例程）；0x6F48 调命令分发器 0x66CC 后操作 [0x40000000] 系外设字（TIM2 域）。

**判决性推论**：

- **换算常数 ×10 在代码不在表** ⇒ 静态表实验连理论翻案可能都没有了——§11.3v 的"静态收官"由反汇编钉死。同时完美解释 T/S 平台期：模板只是账本初值；也解释"改表值 5205(=10×520.5 非 10 倍数)反而差"类噪声形态。
- **35mm 上 @5 显著有效的新解释**：@5 是能力申报里的行程/刻度类参数，改它会经机身 DEL 运算间接改变每步下发量 ⇒ 等效微调"上报轨迹/真实位移"的比值。**真正的直接旋钮 = 这个 ×10**：把它改成 ×9/×11（或换算链任何一环）就是"出厂标定误差"的精确矫正，且对全像面一致生效——这正是 40mm 边缘问题最对症、却只在代码段存在的靶点。
- **运行时 patch 可行性评估（下一阶段主任务）**：①×10 指令替换（3 条 Thumb 指令，bin 内 0x6D42 处有现成空间，如 `movs r3,#N; muls` 或改移位序列）——注意 bl 距离与 sxth 语义保持；②伪造轨迹上报（让 0x06 按 sigrok SEL55210 真值形态逐帧变化——基线数据已在仓内）；③给账本加"上电归零/限位校准"（工程正确做法，工作量最大）。①最小最准，建议先做 **P-Code 世代（代码段 patch）**：×10→×8/×13 两档试 M40 边缘。风险：代码段改错=变砖级（但 bootloader 常驻可救；改动仅 6 字节内、可回滚 ORIG）。
- **（2026-09-05 后记·C 世代实测）**：①兑现——C0(等价) ≡ V3 证明通道清白；曲线 ×8↓/×10基/×12↑(似乎)/×13平 ⇒ 本节"机身真消费 ×N 上报值"的机理链被行为学验证，N 成为**首个运行时可调且有效的旋钮**；真伪（C2 是峰还是噪声）交 D 世代 ×11/×14/×16/×20 判（§11.3w）。若判真：本链其余环节（量化步长 10、账本初值、门控刷新）都是"伪造轨迹上报"（可行性②）的现成插桩点——0x6134 的 `[state+6]` 源变量即帧位置唯一入口，替换其值生成逻辑即可让 0x06 按 SEL55210 形态逐帧变化。

**焦距=同一换算链（D 世代意外收获，行为级实锤）**：用户在机身"光圈输入焦距"界面观察：F6.3 档(40mm)在 D1/D2/D3/D4 上显示 **44/56/64/80mm = 40×N/10 精确同步** ⇒ **焦距上报与位置上报共享 fn@0x6D2C 的 ×N 输出**（调用者复用）。三重意义：① 免费"读值窗口"——不抓包即可从机身显示直接读出环上报的换算值，未来任何换算链 patch 的落地/数值验证零成本；② ~~焦距对 AF 几乎无影响~~ **（收回：用户测 D 时已手动把焦距档设回 40mm 附近，"80mm 显示"未持续存在，且用户实证焦距偏差过大确实影响合焦 ⇒ 焦距声明=活变量，F 世代专测，见 §11.3z）**；③ 混叠警示：历代把"焦距表"当静态声明的推断需修正——它经代码换算。声明源候选=norm05 body+0x18/0x1A 双拷贝 400（=40.0mm，400×N/100 与用户所见精确吻合）。

**fn@0x6D2C 真容（`_extract\re_d2c_xref.py` 实反汇编，2026-09-05）——零参数"门控刷新"例程**：
```
6D2C r3=[0x2000051C]; if ([r3+0xB]==0) return     ; 门: 0x20000527 为 0 直接退出
6D34 [0x2000051C+0xB]=0                            ; 一次性门, 刷完即关
6D3A r1=[0x20000678] ; r2=ldrh [0x2000067C+4]      ; r2=账本 0x20000680
6D42 r3=sxth(r2*10)                                 ; ← C/D 世代 patch 点(唯一乘点)
6D4A r2=[0x20000300]+0x64=0x20000364
6D4E strh r3,[r2+0xC]; strh r3,[r2+0xA]             ; 写 0x20000370 / 0x2000036E
6D52 strh r3,[r1+0x20]; strh r3,[r1+0x1E]           ; 写 [ptr+32] / [ptr+30]
```
三大事实修正/确立：①**四个槽写的是同一个值** ⇒ 旧猜"指针+30/32=当前/目标对"证伪——环在刷新时刻"当前==目标"（对比 Viltrox 0x05 当前/目标两槽可不等，这是形态差异点）；②调用形式：全 bin **无** `bl` 立即数/`0x6D2D` 字面量指向它 ⇒ 调用方式待查（疑经基址寄存器偏移调用 `blx` 目标来自别处或 b.w 短跳，非本轮回旋重点）；③半字 400(0.1mm 焦距候选) 仅命中文件偏移 0x4B9A/4B9C（norm05 body+0x18..1B 区, 值 400 的 LE16 对）——焦距反标定靶点在 norm05 内，待焦距字段精确定位（低优先，焦距证对 AF 无影响）。

**0x06 帧三焦点字段布局（dpreview Entropy512 RE + 仓内捕获复核定案，2026-09-05）**：body[2..3]=焦点马达位置1（所有镜头/环都填）、body[20..21]=马达位置2（原生/部分环填，Viltrox AF 镜头=0 也正常）、**body[26..27]=绝对焦点位置（原生≈110.85×√fp_mm，越近越大；四支真镜头全非零；LM-EA9=0=唯一空缺，运行期镜像不覆写此字段）**。机身用途（推定）：物距计算/距离标尺/MF 辅助触发/**反差 AF 合焦终检坐标系**——G 世代靶点，详见 §11.3ab。

**结论（缩放轴关闭，代码层第一杠杆榨尽）**：全曲线 ×8↓/10基/11↓/12↑(疑噪)/13平/14平/16平/20平——11 与 13 夹住 12 两侧皆回落 = 教科书式孤立尖峰 ⇒ C2 判噪声，按 §11.3w 规则 ② **缩放轴关闭**。行为学旁推：机身对"上报斜率标定误差"不敏感 ⇒ 单纯静态缩放救不了边缘 ⇒ 差异必在**对话节奏/轨迹形态**层（真镜头逐帧变、环只在门开瞬间变）。下一动作不是急着写码，而是**深挖已入库的 5 个 sigrok 捕获的 TXD 命令流**（机身问询节奏、0x06 位置更新的时机结构）——先弄清"机身到底何时消费位置"，再决定伪造轨迹的最小插桩点（候选：门控 0x20000527 常开+假时间基抖动 / 账本初值改写 / 插 b.w 跳转表）。若 TXD 分析证明"位置只在一轮 AF 开头读一次"，则伪造轨迹判死、全力转路线 B 硬件抓包。**（当日兑现：TXD/ Rounds 分析=机身 60Hz 每轮重读 + 真镜头 05.cur 逐帧滑行收敛，消费窗口成立、伪造轨迹判活；据此出 E 世代"门常开"2 字节 patch=释放真实行程轨迹，详见 §11.3y。）**
